# Atomic Executor Memory

- [Project bundle mirror map](project_bundle_mirror_map.md) — TMW has no uniform .claude bundle mirror set; probe per file before syncing.
- [iFile host-bootstrap seam](project_ifile_host_bootstrap_seam.md) — ifile.ts is testable via exported bootstrap/runBootstrap behind a typeof Office guard; do not re-exclude from coverage.
