name: stage-1-format-prettier
on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  stage-1-format-prettier:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/format
