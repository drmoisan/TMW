name: stage-7-integration-vitest
on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  stage-7-integration-vitest:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/integration
