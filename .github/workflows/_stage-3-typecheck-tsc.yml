name: stage-3-typecheck-tsc
on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  stage-3-typecheck-tsc:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/typecheck
