# Implementation Plan — Issue #22: Metadata Schema-Evolution Infrastructure

- **Issue:** #22
- **Feature folder:** `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/`
- **Work Mode:** full-feature
- **Owner:** drmoisan
- **Last Updated:** 2026-05-15T20-44
- **Status:** Complete
- **Evidence root:** `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/`

---

### Phase 0 — Baseline Capture

- [x] [P0-T1] Read `.claude/rules/general-code-change.md` and record the reading in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/baseline/phase0-instructions-read.md` with fields `Timestamp:`, `Policy Order:`, and a list of files read; mark the file as in-progress until all policy files are read.
- [x] [P0-T2] Read `.claude/rules/general-unit-test.md` and append its confirmation to `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/baseline/phase0-instructions-read.md`.
- [x] [P0-T3] Read `.claude/rules/quality-tiers.md` and `.claude/rules/csharp.md` and append their confirmation to `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/baseline/phase0-instructions-read.md`.
- [x] [P0-T4] Read `.claude/rules/architecture-boundaries.md` and mark `Policy Order: complete` in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/baseline/phase0-instructions-read.md`; acceptance: artifact file exists and `Policy Order: complete` is present.
- [x] [P0-T5] Run `dotnet csharpier check .` from repo root; record exit code and output summary in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/baseline/baseline-csharpier.md` with mandatory fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T6] Run `dotnet build TaskMaster.sln --no-incremental` from repo root; record exit code and output summary in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/baseline/baseline-dotnet-build.md` with mandatory fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T7] Run `dotnet test TaskMaster.sln --collect:"XPlat Code Coverage"` from repo root; record exit code, test count, and numeric coverage headline (line %, branch %) in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/baseline/baseline-dotnet-test.md` with mandatory fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` that includes the numeric coverage values.

---

### Phase 1 — NuGet Dependency and Solution Registration

- [x] [P1-T1] Add `<PackageVersion Include="JsonSchema.Net" Version="7.3.1" />` to `Directory.Packages.props` inside the existing `<ItemGroup>` after the `NetArchTest.Rules` line; acceptance: `Directory.Packages.props` contains the `JsonSchema.Net` version entry and `dotnet restore TaskMaster.sln` exits 0.
- [x] [P1-T2] Create `tools/schema-diff/schema-diff.csproj` as a .NET 10 console application (`<OutputType>Exe</OutputType>`) with `<TargetFramework>net10.0</TargetFramework>`, `<Nullable>enable</Nullable>`, `<ImplicitUsings>enable</ImplicitUsings>`, and a `<PackageReference Include="JsonSchema.Net" />`; acceptance: file exists at `tools/schema-diff/schema-diff.csproj` and is a valid MSBuild XML document.
- [x] [P1-T3] Create `tests/TaskMaster.Schema.Tests/TaskMaster.Schema.Tests.csproj` as a .NET 10 xUnit test project with `<TargetFramework>net10.0</TargetFramework>`, `<Nullable>enable</Nullable>`, `<ImplicitUsings>enable</ImplicitUsings>`, package references for `xunit`, `xunit.runner.visualstudio`, `Microsoft.NET.Test.Sdk`, `FluentAssertions`, `coverlet.collector`, `JsonSchema.Net`, `NSubstitute`, `Microsoft.Extensions.TimeProvider.Testing`, and a `<ProjectReference>` to `src/TaskMaster.Infrastructure/TaskMaster.Infrastructure.csproj`; acceptance: file exists and is a valid MSBuild XML document.
- [x] [P1-T4] Run `dotnet sln TaskMaster.sln add tools/schema-diff/schema-diff.csproj tests/TaskMaster.Schema.Tests/TaskMaster.Schema.Tests.csproj` from repo root; acceptance: `TaskMaster.sln` contains both project GUIDs and `dotnet restore TaskMaster.sln` exits 0.
- [x] [P1-T5] Add two entries to `quality-tiers.yml` under the `projects:` list — one for `tests/TaskMaster.Schema.Tests` (name: `TaskMaster.Schema.Tests`, path: `tests/TaskMaster.Schema.Tests`, language: `csharp`, tier: `t4`, rationale: `Test scaffolding (T4): xUnit schema-evolution tests for JSON Schema files and write-path validation. Tier reflects test-infrastructure role.`) and one for `tools/schema-diff` (name: `schema-diff`, path: `tools/schema-diff`, language: `csharp`, tier: `t4`, rationale: `Scaffolding (T4): CLI tool that wraps JsonSchema.Net to perform breaking-change analysis between two schema versions. Used exclusively by the schema-contract CI action.`); acceptance: `quality-tiers.yml` is valid YAML with both entries present.

---

### Phase 2 — JSON Schema Files

- [x] [P2-T1] Create `schemas/v1/classification-result.schema.json` as a JSON Schema 2020-12 document with `"$schema": "https://json-schema.org/draft/2020-12/schema"`, `"type": "object"`, `"required": ["label","confidence"]`, properties `label` (type string, enum `["HighPriority","Promotional","General"]`) and `confidence` (type number, minimum 0.0, maximum 1.0), and `"additionalProperties": false`; acceptance: file is valid JSON and all specified constraints are present.
- [x] [P2-T2] Create `schemas/v1/user-settings.schema.json` as a JSON Schema 2020-12 document with `"$schema": "https://json-schema.org/draft/2020-12/schema"`, `"type": "object"`, `"required": ["userId","notificationsEnabled","triageEnabled","lastModifiedAt"]`, properties `userId` (string), `notificationsEnabled` (boolean), `triageEnabled` (boolean), `lastModifiedAt` (string with `"format": "date-time"`), and `"additionalProperties": false`; acceptance: file is valid JSON and all four required fields are defined.
- [x] [P2-T3] Create `schemas/v1/training-feedback.schema.json` as a JSON Schema 2020-12 document with `"$schema": "https://json-schema.org/draft/2020-12/schema"`, `"type": "object"`, `"required": ["messageId","label","confirmed","recordedAt"]`, properties `messageId` (string), `label` (string), `confirmed` (boolean), `recordedAt` (string with `"format": "date-time"`), and `"additionalProperties": false`; acceptance: file is valid JSON and all four required fields are defined.
- [x] [P2-T4] Create `schemas/v1/task-metadata.schema.json` as a stub JSON Schema 2020-12 document with `"$schema": "https://json-schema.org/draft/2020-12/schema"`, `"$comment": "Stub schema: TaskMetadata type is not yet implemented. Replace this stub before writing any TaskMetadata payload to storage."`, `"type": "object"`, and `"properties": {}`; acceptance: file is valid JSON with the `$comment` field and empty `properties` object.
- [x] [P2-T5] Create `schemas/v1/migration-provenance.schema.json` as a stub JSON Schema 2020-12 document with `"$schema": "https://json-schema.org/draft/2020-12/schema"`, `"$comment": "Stub schema: migration provenance type is not yet implemented. Replace this stub before writing any migration-provenance payload to storage."`, `"type": "object"`, and `"properties": {}`; acceptance: file is valid JSON with the `$comment` field and empty `properties` object.
- [x] [P2-T6] Create `schemas/v1/fixtures/classification-result.valid.json` as a valid JSON object `{"label": "HighPriority", "confidence": 0.92}` that conforms to `schemas/v1/classification-result.schema.json`; acceptance: file is valid JSON.
- [x] [P2-T7] Create `schemas/v1/fixtures/user-settings.valid.json` as a valid JSON object with `userId`, `notificationsEnabled`, `triageEnabled`, and `lastModifiedAt` fields that conforms to `schemas/v1/user-settings.schema.json`; use `"userId": "user-001"`, `"notificationsEnabled": true`, `"triageEnabled": false`, `"lastModifiedAt": "2026-05-15T20:44:00Z"`; acceptance: file is valid JSON.
- [x] [P2-T8] Create `schemas/v1/fixtures/training-feedback.valid.json` as a valid JSON object with `messageId`, `label`, `confirmed`, and `recordedAt` fields that conforms to `schemas/v1/training-feedback.schema.json`; use `"messageId": "msg-001"`, `"label": "General"`, `"confirmed": true`, `"recordedAt": "2026-05-15T20:44:00Z"`; acceptance: file is valid JSON.
- [x] [P2-T9] Create `schemas/v1/fixtures/invalid/classification-result.missing-confidence.json` as an invalid JSON object `{"label": "HighPriority"}` that omits the required `confidence` field; acceptance: file is valid JSON containing `label` but not `confidence`.
- [x] [P2-T10] Create `schemas/v1/fixtures/invalid/user-settings.missing-userId.json` as an invalid JSON object containing `notificationsEnabled`, `triageEnabled`, and `lastModifiedAt` but omitting `userId`; acceptance: file is valid JSON without a `userId` field.
- [x] [P2-T11] Create `schemas/v1/fixtures/invalid/training-feedback.missing-messageId.json` as an invalid JSON object containing `label`, `confirmed`, and `recordedAt` but omitting `messageId`; acceptance: file is valid JSON without a `messageId` field.

---

### Phase 3 — Infrastructure Validator Classes

- [x] [P3-T1] Create `src/TaskMaster.Infrastructure/Validation/SchemaValidationException.cs` in file-scoped namespace `TaskMaster.Infrastructure.Validation` implementing `public sealed class SchemaValidationException : Exception` with properties `public string PayloadType { get; }` and `public IReadOnlyList<string> ValidationErrors { get; }`, a constructor `public SchemaValidationException(string payloadType, IReadOnlyList<string> validationErrors)` that initializes the properties and calls `base($"Payload '{payloadType}' failed schema validation: {string.Join("; ", validationErrors)}")`, and XML doc comments on the public surface; acceptance: file exists, is under 500 lines, and `dotnet build TaskMaster.sln` exits 0 with the file present.
- [x] [P3-T2] Create `src/TaskMaster.Infrastructure/Validation/PayloadSchemaValidator.cs` in file-scoped namespace `TaskMaster.Infrastructure.Validation` implementing `public static class PayloadSchemaValidator` with method `public static void Validate(object payload, string schemaFilePath)` that: (1) reads the schema JSON from `schemaFilePath` via `File.ReadAllText`, (2) parses it via `JsonSchema.Net.JsonSchema.FromText`, (3) serializes `payload` to a `JsonNode` using `JsonSerializer.SerializeToNode(payload, new JsonSerializerOptions(JsonSerializerDefaults.Web))`, (4) calls `schema.Evaluate(node, new EvaluationOptions { OutputFormat = OutputFormat.List })`, (5) collects `result.Details` error messages, (6) throws `SchemaValidationException(payload.GetType().Name, errors)` when `!result.IsValid`; uses `System.Text.Json`, `System.Text.Json.Nodes`, and `JsonSchema.Net` usings; file is under 500 lines; acceptance: `dotnet build TaskMaster.sln` exits 0.
- [x] [P3-T3] Modify `src/TaskMaster.Infrastructure/JsonFileUserSettingsRepository.cs` — add `using TaskMaster.Infrastructure.Validation;` at the top; in `SaveAsync`, insert `PayloadSchemaValidator.Validate(settings, GetSchemaPath("user-settings.schema.json"));` immediately after `ArgumentNullException.ThrowIfNull(settings)` and before `await _lock.WaitAsync(ct)`; add a private static method `private static string GetSchemaPath(string schemaFileName)` that computes `Path.GetFullPath(Path.Combine(AppContext.BaseDirectory, "..", "..", "..", "..", "..", "schemas", "v1", schemaFileName))` and returns the result; acceptance: file compiles with no errors, `SaveAsync` signature is unchanged, and the method body contains the `PayloadSchemaValidator.Validate` call.
- [x] [P3-T4] Modify `src/TaskMaster.Infrastructure/InMemoryTrainingRepository.cs` — add `using TaskMaster.Infrastructure.Validation;` at the top; in `RecordAsync`, insert `PayloadSchemaValidator.Validate(feedback, GetSchemaPath("training-feedback.schema.json"));` immediately after `ArgumentNullException.ThrowIfNull(feedback)` and before the stamp-and-enqueue line; add a private static method `private static string GetSchemaPath(string schemaFileName)` that computes the same path resolution as in P3-T3; acceptance: file compiles with no errors, `RecordAsync` signature is unchanged, and the method body contains the `PayloadSchemaValidator.Validate` call.
- [x] [P3-T5] Run `dotnet build TaskMaster.sln --no-incremental` from repo root after P3-T1 through P3-T4 to confirm all modified and new infrastructure files compile with zero errors and zero warnings; acceptance: exit code 0.

---

### Phase 4 — Schema Test Project

- [x] [P4-T1] Create `tests/TaskMaster.Schema.Tests/ClassificationResultSchemaTests.cs` in file-scoped namespace `TaskMaster.Schema.Tests` with xUnit test class `ClassificationResultSchemaTests` containing three `[Fact]` tests: (a) `SchemaFile_IsWellFormed` — reads `schemas/v1/classification-result.schema.json` and calls `JsonSchema.FromText`; asserts no exception is thrown; (b) `ValidFixture_PassesSchema` — loads the valid fixture from `schemas/v1/fixtures/classification-result.valid.json`, parses as `JsonNode`, evaluates against the schema; asserts `result.IsValid` is true; (c) `InvalidFixture_MissingConfidence_FailsSchema` — loads `schemas/v1/fixtures/invalid/classification-result.missing-confidence.json`, evaluates against the schema; asserts `result.IsValid` is false; schema and fixture paths are resolved via `Path.GetFullPath(Path.Combine(AppContext.BaseDirectory, "..", "..", "..", "..", "..", "schemas", "v1", ...))` to be deterministic regardless of working directory; file is under 500 lines; acceptance: file compiles and all three tests pass via `dotnet test tests/TaskMaster.Schema.Tests/TaskMaster.Schema.Tests.csproj`.
- [x] [P4-T2] Create `tests/TaskMaster.Schema.Tests/UserSettingsSchemaTests.cs` in file-scoped namespace `TaskMaster.Schema.Tests` with xUnit test class `UserSettingsSchemaTests` containing four tests: (a) `[Fact] SchemaFile_IsWellFormed`; (b) `[Fact] ValidFixture_PassesSchema`; (c) `[Fact] InvalidFixture_MissingUserId_FailsSchema` — validates that `schemas/v1/fixtures/invalid/user-settings.missing-userId.json` fails the `user-settings.schema.json` schema; (d) `[Fact] SaveAsync_ThrowsSchemaValidationException_WhenUserIdIsEmpty` — constructs a `JsonFileUserSettingsRepository` using `NSubstitute` substitutes for `IFileWriter` and `IOptions<UserSettingsFileOptions>` and a `FakeTimeProvider`; calls `SaveAsync` with a `UserSettings` record that has an empty `UserId` string (which will serialize to `""` and may or may not fail schema depending on schema constraints) OR passes a record that is structurally valid but has an extra property to trip `additionalProperties: false` — the test must produce a failing scenario; use `FluentAssertions` `await act.Should().ThrowAsync<SchemaValidationException>()`; file is under 500 lines; acceptance: all four tests pass.
- [x] [P4-T3] Create `tests/TaskMaster.Schema.Tests/TrainingFeedbackSchemaTests.cs` in file-scoped namespace `TaskMaster.Schema.Tests` with xUnit test class `TrainingFeedbackSchemaTests` containing four tests: (a) `[Fact] SchemaFile_IsWellFormed`; (b) `[Fact] ValidFixture_PassesSchema`; (c) `[Fact] InvalidFixture_MissingMessageId_FailsSchema` — validates that `schemas/v1/fixtures/invalid/training-feedback.missing-messageId.json` fails the `training-feedback.schema.json` schema; (d) `[Fact] RecordAsync_ThrowsSchemaValidationException_WhenPayloadMissingMessageId` — creates an `InMemoryTrainingRepository` with `FakeTimeProvider` and calls `RecordAsync` with a `TrainingFeedback` record whose `MessageId` is an empty string; asserts `SchemaValidationException` is thrown; if `InMemoryTrainingRepository` is `internal`, add `<InternalsVisibleTo Include="TaskMaster.Schema.Tests" />` to `src/TaskMaster.Infrastructure/TaskMaster.Infrastructure.csproj` first; file is under 500 lines; acceptance: all four tests pass.
- [x] [P4-T4] If test P4-T3(d) requires `InternalsVisibleTo`, add `<InternalsVisibleTo Include="TaskMaster.Schema.Tests" />` to `src/TaskMaster.Infrastructure/TaskMaster.Infrastructure.csproj` inside a new `<ItemGroup>`; acceptance: `dotnet build TaskMaster.sln` exits 0 after the addition.
- [x] [P4-T5] Create `tests/TaskMaster.Schema.Tests/SchemaCompatibilityTests.cs` in file-scoped namespace `TaskMaster.Schema.Tests` with xUnit test class `SchemaCompatibilityTests` containing: (a) `[Fact] SchemaFilesExist_ForAllV1Types` — asserts that all five expected schema files exist in `schemas/v1/`: `classification-result.schema.json`, `user-settings.schema.json`, `training-feedback.schema.json`, `task-metadata.schema.json`, `migration-provenance.schema.json`; (b) `[Theory] [InlineData("classification-result")] [InlineData("user-settings")] [InlineData("training-feedback")] void V1Fixture_PassesV1Schema(string payloadType)` — loads `schemas/v1/fixtures/<payloadType>.valid.json` and validates it against `schemas/v1/<payloadType>.schema.json`, asserting `result.IsValid` is true; (c) `[Fact] BackwardCompatibility_SingleVersion_VacuousPass` — a fact with no production assertions beyond a `// v1 only: backward-compat test is vacuous when no prior version exists` comment followed by `true.Should().BeTrue()`, acting as a harness placeholder for when v2 is introduced; file is under 500 lines; acceptance: all tests pass via `dotnet test tests/TaskMaster.Schema.Tests/TaskMaster.Schema.Tests.csproj`.
- [x] [P4-T6] Run `dotnet test tests/TaskMaster.Schema.Tests/TaskMaster.Schema.Tests.csproj --collect:"XPlat Code Coverage"` and record exit code, test count, and coverage headline in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/qa-gates/schema-tests-initial-run.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric coverage values; acceptance: all tests pass (exit code 0).

---

### Phase 5 — Architecture Tests Verification

- [x] [P5-T1] Run `dotnet test tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj --no-build` after Phase 3 changes to confirm all existing architecture boundary tests still pass; record exit code and any failures in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/qa-gates/architecture-tests-verification.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`; acceptance: exit code 0 and zero test failures.
- [x] [P5-T2] If P5-T1 fails due to the new `TaskMaster.Infrastructure.Validation` namespace being excluded by any explicit namespace allowlist in `tests/TaskMaster.ArchitectureTests/LayerBoundaryTests.cs` or `tests/TaskMaster.ArchitectureTests/NoComArchitectureTests.cs`, update the affected test to include `TaskMaster.Infrastructure.Validation` in its allowed set and re-run until P5-T1 exits 0; acceptance: `dotnet test tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj` exits 0.

---

### Phase 6 — Schema-Diff Tool

- [x] [P6-T1] Create `tools/schema-diff/Program.cs` in file-scoped namespace `SchemaDiff` implementing a .NET 10 top-level program that: (1) parses CLI arguments `--current <path>` and `--baseline <path>`, exits 2 with an error message if either is missing or malformed; (2) reads both schema files using `File.ReadAllText`, exits 2 if either file is not found or cannot be parsed as JSON Schema via `JsonSchema.Net.JsonSchema.FromText`; (3) compares the two schemas by checking: removal of any property from the baseline `required` array (breaking), addition of any property to the `required` array that was not required in baseline (breaking), change of `additionalProperties` from absent or `true` to `false` (breaking), removal of any property definition that was present in baseline (breaking); (4) if breaking changes detected, prints each to stdout and exits 1; (5) if no breaking changes, exits 0; stub schemas (those with empty `properties` object and a `$comment` containing `"Stub schema"`) are skipped without comparison; file must not exceed 500 lines; acceptance: file exists at `tools/schema-diff/Program.cs` and `dotnet build tools/schema-diff/schema-diff.csproj` exits 0.
- [x] [P6-T2] Run `dotnet run --project tools/schema-diff -- --current schemas/v1/user-settings.schema.json --baseline schemas/v1/user-settings.schema.json` from repo root and confirm exit code is 0; record outcome in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/qa-gates/schema-diff-smoke-no-change.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P6-T3] Create a temporary file `schemas/v1/classification-result.schema.json.breaking-test.tmp` with the `confidence` property removed from the `required` array; run `dotnet run --project tools/schema-diff -- --current schemas/v1/classification-result.schema.json.breaking-test.tmp --baseline schemas/v1/classification-result.schema.json` and confirm exit code is 1; delete the temp file; record outcome in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/qa-gates/schema-diff-smoke-breaking-detected.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`; acceptance: exit code is exactly 1 and stdout contains the breaking change description.

---

### Phase 7 — CI Integration

- [x] [P7-T1] Create `.github/actions/schema-contract/action.yml` as a composite action with the following structure: `name: Schema Contract`, `description: Checks for breaking JSON Schema changes in schemas/ using the schema-diff .NET tool.`, `runs.using: composite`; steps: (1) Setup .NET 10 via `actions/setup-dotnet@v4`; (2) `dotnet restore TaskMaster.sln`; (3) `dotnet build tools/schema-diff/schema-diff.csproj --no-restore`; (4) a bash script step that iterates over all `*.schema.json` files under `schemas/`, for each attempts `git show "origin/${{ github.base_ref }}:<relative-path>"` to extract the baseline to a temp file, skips if no baseline (first-PR pattern), and runs `dotnet run --project tools/schema-diff -- --current <path> --baseline <temp-path>` capturing exit code — fails the action with `exit 1` and prints the offending schema path if the tool returns 1; exits 2 propagation is treated as tool error and also fails the action; (5) a final step that echos a summary of checked schemas; acceptance: file is syntactically valid YAML, contains all five steps, and is parseable without error.
- [x] [P7-T2] Modify `.github/workflows/pr-pipeline.yml` — in the `stage-6-contract` job body, after the step `- uses: ./.github/actions/contract`, add a new step `- uses: ./.github/actions/schema-contract`; the `stage-6-contract` job already has `fetch-depth: 0` (required for `git show` in the schema-contract action); acceptance: the modified YAML is valid, the new action call appears after the OpenAPI contract call, and no other job structure is changed.
- [x] [P7-T3] Modify `.github/workflows/pre-merge-pipeline.yml` — add a new job `stage-14-schema-evolution` at the end of the jobs section with: `needs: [stage-10-e2e]`, `runs-on: windows-latest`; steps: `uses: actions/checkout@v4`, Setup .NET 10 via `actions/setup-dotnet@v4` with `dotnet-version: 10.0.x`, `dotnet restore TaskMaster.sln`, `dotnet test tests/TaskMaster.Schema.Tests/TaskMaster.Schema.Tests.csproj --no-restore --collect:"XPlat Code Coverage"`; acceptance: the modified YAML is valid and the new job is sequenced after `stage-10-e2e`.

---

### Phase 8 — Full QA Loop

- [x] [P8-T1] Run `dotnet csharpier .` from repo root to auto-format all C# source files modified in this feature; acceptance: command exits 0.
- [x] [P8-T2] Run `dotnet csharpier check .` from repo root to confirm all C# files are correctly formatted after P8-T1; record exit code and output in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/qa-gates/qa-csharpier.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`; if exit code is non-zero, fix formatting and restart this phase from P8-T1.
- [x] [P8-T3] Run `dotnet build TaskMaster.sln --no-incremental` from repo root to confirm the entire solution builds with zero errors and zero warnings (nullable analysis and analyzers included); record exit code and output in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/qa-gates/qa-dotnet-build.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`; if exit code is non-zero, fix errors and restart from P8-T1.
- [x] [P8-T4] Run `dotnet test tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj --no-build` from repo root to confirm architecture tests pass after all changes; record exit code and test count in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/qa-gates/qa-architecture-tests.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`; if exit code is non-zero, fix violations and restart from P8-T1.
- [x] [P8-T5] Run `dotnet test TaskMaster.sln --collect:"XPlat Code Coverage" --no-build` from repo root to execute all tests in the solution including the new `TaskMaster.Schema.Tests`; record exit code, total test count, pass/fail breakdown, and numeric coverage headline (line %, branch %) in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/qa-gates/qa-dotnet-test-coverage.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` that includes numeric coverage values; confirm line coverage >= 85% and branch coverage >= 75% across the solution; if exit code is non-zero or coverage falls below thresholds, fix and restart from P8-T1.
- [x] [P8-T6] Run `dotnet run --project tools/schema-diff -- --current schemas/v1/classification-result.schema.json --baseline schemas/v1/classification-result.schema.json` to confirm the tool exits 0 on identical schemas; record outcome in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/qa-gates/qa-schema-diff-no-break.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P8-T7] Confirm `quality-tiers.yml` is structurally valid by verifying via YAML parse (or running `.github/scripts/validate-quality-tiers.ps1` if accessible) that `tests/TaskMaster.Schema.Tests` and `tools/schema-diff` entries are present, well-formed, and have tier values of `t4`; record outcome in `docs/features/active/2026-05-15-metadata-schema-evolution-infra-22/evidence/qa-gates/qa-quality-tiers-validation.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.

---

## Acceptance Criteria Traceability

| Acceptance Criterion (from issue.md / spec.md) | Plan Tasks |
|---|---|
| `/schemas/v1/` contains JSON Schema files for all required payload types | P2-T1, P2-T2, P2-T3, P2-T4, P2-T5 |
| Backend write paths reject payloads that fail schema validation (test coverage required) | P3-T3, P3-T4, P4-T2(d), P4-T3(d) |
| Forward-compat tests: v1 fixtures are readable by v1 code | P4-T5(b) |
| Backward-compat tests: payloads written by current code pass the N-1 schema | P4-T5(c) (vacuous pass while only v1 exists; harness ready for v2) |
| PR pipeline stage 6 blocks merge on a breaking schema change without a version bump | P7-T1, P7-T2 |
| Nightly pipeline stage 14 runs schema-evolution tests against the last three schema versions | P7-T3 |
| An incompatible schema change without a version bump is detected and blocks the build | P6-T1, P6-T3 |
| A version bump with compat fixture passes (new schema dir has no baseline, first-PR pattern passes) | P7-T1 (first-PR skip logic in the action), P6-T2 |
