# Plan — Issue #7: Establish .NET Foundation (Prompt C1)

- Work Mode: full-feature
- Feature folder: `docs/features/active/2026-05-10-establish-dotnet-foundation-7/`
- Canonical issue: #7
- Evidence root: `docs/features/active/2026-05-10-establish-dotnet-foundation-7/evidence/`
- Plan-path continuity: this file is updated in place across preflight revision loops.

## Conventions

- Evidence artifacts MUST be written under the canonical evidence root above per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Forbidden locations (`artifacts/baselines/`, `artifacts/baseline/`, `artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/evidence/`, `artifacts/coverage/`, `artifacts/post-change/`) MUST NOT be used. Note: `artifacts/openapi/current.json` is product output (NSwag emission), not evidence, and remains allowed.
- ISO-8601 timestamp format: `yyyy-MM-ddTHH-mm`. Substitute `<timestamp>` with the current value when executing.
- Every command-bearing task writes an evidence artifact containing `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- Toolchain order (per `.claude/rules/general-code-change.md`): format -> lint -> type-check -> architecture -> unit tests -> contract -> integration. Stages 6 and 7 are out of scope for this issue.
- File-size budget: no production/test file may exceed 500 lines. Markdown rule files are exempt.
- Per-task budget: 1-3 production/config files plus their tests. Each `[P#-T#]` is a single binary outcome with one verifiable acceptance criterion (AC tag).

## Mirror Discipline (Mandatory)

- Every `.claude/rules/*.md` edit MUST be mirrored in the corresponding `.github/instructions/*.instructions.md` file inside the same phase.
- Every `.claude/skills/<name>/SKILL.md` edit that has a `.github/skills/<name>/SKILL.md` mirror MUST update both files inside the same phase.
- Audit of existing mirrors (verified during planning):
  - `.github/skills/feature-review-workflow/SKILL.md` exists -> mirror update required (P3).
  - `.github/skills/csharp-qa-gate/` does NOT exist -> no mirror update required for this skill (P3 contains an explicit absence-check task).
  - `.github/skills/invoke-csharp-engineer/` does NOT exist -> no mirror update required for this skill (P3 contains an explicit absence-check task).

## Acceptance Criteria Coverage Map (informational)

- AC1-AC7: Phase 1
- AC8-AC9: Phase 2
- AC10-AC14: Phase 3
- AC15-AC16, AC19: Phase 5
- AC17-AC18: Phase 6
- AC20, AC22-AC23: Phase 7
- AC21: Phases 4 and 8
- AC24: Phase 9
- AC25: Phase 10
- AC30: Phase 11
- AC26: Phase 12
- AC27-AC29: Phase 13
- AC1-AC30 final reconciliation: Phase 14

---

### Phase 0 — Preflight and Scope Validation

- [x] [P0-T1] Read the policy stack in the exact order defined by `.claude/skills/policy-compliance-order/SKILL.md`: `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/architecture-boundaries.md`, `.claude/rules/csharp.md`, `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Also record an absence check for `CLAUDE.md` at repo root with `SearchScope: repo root`, `SearchPatterns: CLAUDE.md`, `SearchResult: none` (or the path if present). Write `docs/features/active/2026-05-10-establish-dotnet-foundation-7/evidence/baseline/phase0-instructions-read.<timestamp>.md` with `Timestamp:`, `Policy Order:` (the exact list above plus the CLAUDE.md absence record), and the explicit list of files read. PASS: artifact exists, lists the six existing files as read, and includes the CLAUDE.md absence record.
- [x] [P0-T2] Read `docs/features/active/2026-05-10-establish-dotnet-foundation-7/issue.md`, `spec.md`, and `user-story.md` in full. Write `evidence/baseline/phase0-inputs-read.<timestamp>.md` listing all three paths and confirming `AC count = 30`. PASS: artifact exists and references `AC count = 30`.
- [x] [P0-T3] Confirm presence of the eight Phase 1 / operational artifact files that will be edited: `.claude/rules/csharp.md`, `.github/instructions/csharp-code-change.instructions.md`, `.github/instructions/csharp-unit-test.instructions.md`, `.claude/agents/csharp-typed-engineer.md`, `.claude/skills/csharp-qa-gate/SKILL.md`, `.claude/skills/invoke-csharp-engineer/SKILL.md`, `.claude/skills/feature-review-workflow/SKILL.md`, `.github/agents/csharp-typed-engineer.agent.md`. Write `evidence/baseline/phase0-target-files-present.<timestamp>.md` with one `Path:` / `Exists:` line per file. PASS: all eight `Exists: true`.
- [x] [P0-T4] Enumerate `.github/skills/` to confirm mirror status: search `.github/skills/csharp-qa-gate/`, `.github/skills/invoke-csharp-engineer/`, `.github/skills/feature-review-workflow/`. Write `evidence/baseline/phase0-mirror-audit.<timestamp>.md` with `SearchScope: .github/skills/`, `SearchPatterns: csharp-qa-gate/SKILL.md, invoke-csharp-engineer/SKILL.md, feature-review-workflow/SKILL.md`, `SearchResult:` for each. PASS: artifact lists existence per mirror; this is the authoritative source for mirror obligations in P3.
- [x] [P0-T5] Capture pre-change baseline grep over the rule + instruction + skill + agent files for forbidden tokens. Command (PowerShell): `Select-String -Path '.claude/rules/csharp.md','.github/instructions/csharp-code-change.instructions.md','.github/instructions/csharp-unit-test.instructions.md','.claude/agents/csharp-typed-engineer.md','.claude/skills/csharp-qa-gate/SKILL.md','.claude/skills/invoke-csharp-engineer/SKILL.md','.claude/skills/feature-review-workflow/SKILL.md','.github/agents/csharp-typed-engineer.agent.md' -Pattern 'MSTest','TestClass','TestMethod','\bMoq\b','vstest\.console','TaskMaster\.sln','msbuild' -SimpleMatch:$false`. Capture stdout to `evidence/baseline/baseline-grep-forbidden-tokens.<timestamp>.txt` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (record the count of matches per token; this is the pre-change baseline). PASS: artifact exists with non-empty match list (baseline expectation: matches exist before Phase 1 work begins).
- [x] [P0-T6] Verify absence of solution skeleton and infra config files that Phases 4-7 will create. Search: `TaskMaster.sln`, `Directory.Build.props`, `Directory.Packages.props`, `BannedSymbols.txt`, `.config/dotnet-tools.json`, `**/*.csproj`. Write `evidence/baseline/phase0-dotnet-absence.<timestamp>.md` with `SearchScope: repo root`, `SearchPatterns:` listing each, `SearchResult: none` (expected) or actual paths found. PASS: artifact written; any unexpected hits are flagged for P0 follow-up before Phase 4 begins.
- [x] [P0-T7] Capture `quality-tiers.yml` baseline content into `evidence/baseline/quality-tiers-baseline.<timestamp>.md` (raw copy with header). PASS: artifact written for diff reference in Phase 11.
- [x] [P0-T8] Capture `.github/workflows/pr-pipeline.yml` baseline content into `evidence/baseline/pr-pipeline-baseline.<timestamp>.md` (raw copy with header). PASS: artifact written for diff reference in Phase 10.
- [x] [P0-T9] Phase 0 closure gate: write `evidence/baseline/phase0-closure.<timestamp>.md` listing every Phase 0 artifact filename produced. PASS: artifact exists and references P0-T1 through P0-T8 artifacts by name.

---

### Phase 1 — C# Rule Prose Rewrite (`.claude/rules/csharp.md`)

Each task touches only `.claude/rules/csharp.md`. After P1-T8 the mirror obligation applies in Phase 2.

- [x] [P1-T1] Edit `.claude/rules/csharp.md` to replace MSTest references with xUnit throughout (no `[TestClass]`, `[TestMethod]`, `MSTest`, or `vstest.console` tokens remain in active prose). Verify: `Select-String -Path '.claude/rules/csharp.md' -Pattern 'MSTest','TestClass','TestMethod','vstest\.console' -SimpleMatch:$false`. Capture to `evidence/qa-gates/p1-t1-grep.<timestamp>.txt`. PASS: zero matches. AC: AC1.
- [x] [P1-T2] Edit `.claude/rules/csharp.md` to replace `Moq` with `NSubstitute` everywhere and replace `TaskMaster.sln`/`msbuild` references with `dotnet build`. Verify: grep over the file for `\bMoq\b`, `TaskMaster\.sln`, `msbuild` returns zero matches. Capture to `evidence/qa-gates/p1-t2-grep.<timestamp>.txt`. PASS: zero matches. AC: AC1.
- [x] [P1-T3] Edit `.claude/rules/csharp.md` to add an `## Analyzer Stack` section listing exactly six packages: `Meziantou.Analyzer`, `SonarAnalyzer.CSharp`, `Roslynator.Analyzers`, `AsyncFixer`, `SecurityCodeScan.VS2019`, `Microsoft.CodeAnalysis.BannedApiAnalyzers`, each marked `PrivateAssets="all"`. Verify: `Select-String -Path '.claude/rules/csharp.md' -Pattern 'Meziantou\.Analyzer','SonarAnalyzer\.CSharp','Roslynator\.Analyzers','AsyncFixer','SecurityCodeScan\.VS2019','Microsoft\.CodeAnalysis\.BannedApiAnalyzers'` shows six hits and `Select-String -Pattern 'PrivateAssets="all"'` shows >=1 hit in same section. Capture to `evidence/qa-gates/p1-t3-grep.<timestamp>.txt`. PASS: all six packages and `PrivateAssets="all"` present. AC: AC2.
- [x] [P1-T4] Edit `.claude/rules/csharp.md` `## DI Seams` section so it names `TimeProvider` as preferred and `Microsoft.Extensions.TimeProvider.Testing` for tests; `IClock` marked legacy/pre-.NET 8 only. Verify: `Select-String -Path '.claude/rules/csharp.md' -Pattern 'TimeProvider','Microsoft\.Extensions\.TimeProvider\.Testing','IClock.*legacy|legacy.*IClock'` shows all three present. Capture to `evidence/qa-gates/p1-t4-grep.<timestamp>.txt`. PASS: three matches confirmed. AC: AC3.
- [x] [P1-T5] Edit `.claude/rules/csharp.md` to add a `## Banned APIs` section referencing `BannedSymbols.txt` and naming the five banned APIs: `DateTime.Now`, `DateTime.UtcNow`, `Random.Shared`, `Thread.Sleep`, `Task.Delay`. Verify: grep for `BannedSymbols\.txt` and each of the five names returns hits. Capture to `evidence/qa-gates/p1-t5-grep.<timestamp>.txt`. PASS: six matches minimum. AC: AC4.
- [x] [P1-T6] Edit `.claude/rules/csharp.md` coverage rule so it states "line >= 85% and branch >= 75% uniform across tiers" and "mutation >= 75% on T1". Verify: grep for `line >= 85%`, `branch >= 75%`, `uniform`, `mutation >= 75%`. Capture to `evidence/qa-gates/p1-t6-grep.<timestamp>.txt`. PASS: all four phrases present. AC: AC5.
- [x] [P1-T7] Edit `.claude/rules/csharp.md` to add a `## Property-Based and Mutation Testing` subsection naming `CsCheck` (>=1 property test per pure function on T1/T2) and `Stryker.NET` (T1, >=75% mutation score). Verify: grep for `CsCheck`, `Stryker\.NET`. Capture to `evidence/qa-gates/p1-t7-grep.<timestamp>.txt`. PASS: both present. AC: AC6.
- [x] [P1-T8] Edit `.claude/rules/csharp.md` to add a `## Golden Tests` subsection naming `Verify.Xunit` for T1 classifier-output modules. Verify: grep for `Verify\.Xunit`. Capture to `evidence/qa-gates/p1-t8-grep.<timestamp>.txt`. PASS: one or more matches. AC: AC7.
- [x] [P1-T9] Phase 1 closure gate: re-run the forbidden-token grep used in P0-T5 against only `.claude/rules/csharp.md`. Command: `Select-String -Path '.claude/rules/csharp.md' -Pattern 'MSTest','TestClass','TestMethod','\bMoq\b','vstest\.console','TaskMaster\.sln','msbuild'`. Capture to `evidence/qa-gates/phase1-grep.<timestamp>.txt`. PASS: zero matches. AC: AC1.

---

### Phase 2 — Instruction-File Mirror

Mirror Phase 1 edits into both instruction files in the same phase, per Mirror Discipline.

- [x] [P2-T1] Edit `.github/instructions/csharp-code-change.instructions.md` to mirror the test-framework/toolchain renames from P1-T1 and P1-T2 (no MSTest/Moq/TaskMaster.sln/vstest.console/msbuild in active text). Verify: `Select-String -Path '.github/instructions/csharp-code-change.instructions.md' -Pattern 'MSTest','TestClass','TestMethod','\bMoq\b','vstest\.console','TaskMaster\.sln','msbuild'`. Capture to `evidence/qa-gates/p2-t1-grep.<timestamp>.txt`. PASS: zero matches. AC: AC8.
- [x] [P2-T2] Edit `.github/instructions/csharp-code-change.instructions.md` to mirror the analyzer-stack section, DI seams update, banned APIs section, coverage rule, and CsCheck/Stryker.NET/Verify.Xunit subsections from P1-T3 through P1-T8. Verify: grep for `Meziantou\.Analyzer`, `SonarAnalyzer\.CSharp`, `Roslynator\.Analyzers`, `AsyncFixer`, `SecurityCodeScan\.VS2019`, `Microsoft\.CodeAnalysis\.BannedApiAnalyzers`, `TimeProvider`, `Microsoft\.Extensions\.TimeProvider\.Testing`, `BannedSymbols\.txt`, `line >= 85%`, `branch >= 75%`, `CsCheck`, `Stryker\.NET`, `Verify\.Xunit`. Capture to `evidence/qa-gates/p2-t2-grep.<timestamp>.txt`. PASS: all 14 patterns produce matches. AC: AC8.
- [x] [P2-T3] Edit `.github/instructions/csharp-unit-test.instructions.md` to require xUnit, `[Fact]`, `[Theory]`, `[InlineData]`, `IClassFixture<T>`, and NSubstitute; remove any MSTest/Moq vocabulary. Verify: grep for `xUnit`, `\[Fact\]`, `\[Theory\]`, `\[InlineData\]`, `IClassFixture<T>`, `NSubstitute` returns matches and grep for `MSTest`, `TestClass`, `TestMethod`, `\bMoq\b` returns zero matches. Capture to `evidence/qa-gates/p2-t3-grep.<timestamp>.txt`. PASS: six required patterns matched; four forbidden patterns return zero matches. AC: AC9.
- [x] [P2-T4] Edit `.github/instructions/csharp-unit-test.instructions.md` to mirror the coverage rule (line >= 85%, branch >= 75% uniform across tiers; mutation >= 75% on T1) and reference TimeProvider/FakeTimeProvider for clock seam in tests. Verify: grep for `line >= 85%`, `branch >= 75%`, `uniform`, `mutation >= 75%`, `TimeProvider`, `FakeTimeProvider`. Capture to `evidence/qa-gates/p2-t4-grep.<timestamp>.txt`. PASS: all six patterns matched. AC: AC9.
- [x] [P2-T5] Phase 2 closure gate: re-run the forbidden-token grep across both instruction files combined. Command: `Select-String -Path '.github/instructions/csharp-code-change.instructions.md','.github/instructions/csharp-unit-test.instructions.md' -Pattern 'MSTest','TestClass','TestMethod','\bMoq\b','vstest\.console','TaskMaster\.sln','msbuild'`. Capture to `evidence/qa-gates/phase2-grep.<timestamp>.txt`. PASS: zero matches. AC: AC8, AC9.

---

### Phase 3 — Operational Artifact Updates

Per the P0-T4 mirror audit: only `feature-review-workflow` has a `.github/skills/` mirror among the targeted skills. Tasks below treat that as the only required mirror copy.

- [x] [P3-T1] Edit `.claude/agents/csharp-typed-engineer.md`: frontmatter `description:` value must contain the exact phrase `xUnit toolchain`; body must reference `xUnit`, `NSubstitute`, and `dotnet build` (replacing any MSTest/Moq/msbuild prose). Verify: `Select-String -Path '.claude/agents/csharp-typed-engineer.md' -Pattern 'xUnit toolchain','xUnit','NSubstitute','dotnet build'` shows matches; `Select-String -Pattern 'MSTest','\bMoq\b','msbuild'` shows zero matches. Capture to `evidence/qa-gates/p3-t1-grep.<timestamp>.txt`. PASS: required patterns present, forbidden patterns absent. AC: AC10.
- [x] [P3-T2] Edit `.claude/skills/csharp-qa-gate/SKILL.md`: replace `msbuild` build prose with `dotnet build` and replace `vstest.console` test prose with `dotnet test --collect:"XPlat Code Coverage"`; replace MSTest references with xUnit; add an explicit step that runs architecture tests (`dotnet test` against the `*.ArchitectureTests` project). Note: Directory.Build.props centralizes `Nullable=enable` and `TreatWarningsAsErrors=true`, so the skill must no longer prescribe per-project `EnableNETAnalyzers`/`EnforceCodeStyleInBuild` settings. Verify: grep for `dotnet build`, `dotnet test --collect:"XPlat Code Coverage"`, `xUnit`, `ArchitectureTests` returns matches; grep for `msbuild`, `vstest\.console`, `MSTest`, `EnableNETAnalyzers`, `EnforceCodeStyleInBuild` returns zero matches. Capture to `evidence/qa-gates/p3-t2-grep.<timestamp>.txt`. PASS: four required patterns matched; five forbidden patterns absent. AC: AC11.
- [x] [P3-T3] Confirm absence of `.github/skills/csharp-qa-gate/` mirror (per P0-T4); write `evidence/qa-gates/p3-t3-mirror-absence.<timestamp>.md` with `SearchScope: .github/skills/csharp-qa-gate/`, `SearchPatterns: SKILL.md`, `SearchResult: none`. PASS: artifact written; no mirror edit required. AC: AC11 (mirror discipline verified).
- [x] [P3-T4] Edit `.claude/skills/invoke-csharp-engineer/SKILL.md`: frontmatter and step text must read `CSharpier -> .NET Analyzers -> Nullable Analysis -> xUnit` (in that order) everywhere a toolchain order is named. Verify: `Select-String -Path '.claude/skills/invoke-csharp-engineer/SKILL.md' -Pattern 'CSharpier -> \.NET Analyzers -> Nullable Analysis -> xUnit'` returns at least one match; `Select-String -Pattern 'MSTest','\bMoq\b'` returns zero matches. Capture to `evidence/qa-gates/p3-t4-grep.<timestamp>.txt`. PASS: required ordering present, forbidden tokens absent. AC: AC12.
- [x] [P3-T5] Confirm absence of `.github/skills/invoke-csharp-engineer/` mirror (per P0-T4); write `evidence/qa-gates/p3-t5-mirror-absence.<timestamp>.md` with `SearchScope`, `SearchPatterns`, `SearchResult: none`. PASS: artifact written; no mirror edit required. AC: AC12.
- [x] [P3-T6] Edit `.claude/skills/feature-review-workflow/SKILL.md`: any C# coverage command line must use `dotnet test --collect:"XPlat Code Coverage"`. Verify: `Select-String -Path '.claude/skills/feature-review-workflow/SKILL.md' -Pattern 'dotnet test --collect:"XPlat Code Coverage"'` >=1 match; grep for `vstest\.console` zero matches. Capture to `evidence/qa-gates/p3-t6-grep.<timestamp>.txt`. PASS: required present, forbidden absent. AC: AC13.
- [x] [P3-T7] Mirror P3-T6 into `.github/skills/feature-review-workflow/SKILL.md` (mirror exists per P0-T4). Verify the same grep applied to the mirror file. Capture to `evidence/qa-gates/p3-t7-grep.<timestamp>.txt`. PASS: required present, forbidden absent. AC: AC13 (mirror discipline).
- [x] [P3-T8] Edit `.github/agents/csharp-typed-engineer.agent.md`: replace every MSTest reference with xUnit and every Moq reference with NSubstitute, including example code snippets (e.g., `[TestClass]`/`[TestMethod]` -> `[Fact]`/`[Theory]`; `Mock<IService>` -> `Substitute.For<IService>()`). Verify: grep for `xUnit`, `NSubstitute`, `\[Fact\]` matches; grep for `MSTest`, `\bMoq\b`, `TestClass`, `TestMethod`, `Mock<` returns zero matches. Capture to `evidence/qa-gates/p3-t8-grep.<timestamp>.txt`. PASS: required present, forbidden absent. AC: AC14.
- [x] [P3-T9] Phase 3 closure gate: re-run the forbidden-token grep across all five operational artifact files combined (`.claude/agents/csharp-typed-engineer.md`, `.claude/skills/csharp-qa-gate/SKILL.md`, `.claude/skills/invoke-csharp-engineer/SKILL.md`, `.claude/skills/feature-review-workflow/SKILL.md`, `.github/skills/feature-review-workflow/SKILL.md`, `.github/agents/csharp-typed-engineer.agent.md`). Pattern set: `MSTest`, `TestClass`, `TestMethod`, `\bMoq\b`, `vstest\.console`, `TaskMaster\.sln`, `msbuild`. Capture to `evidence/qa-gates/phase3-grep.<timestamp>.txt`. PASS: zero matches across the set. AC: AC10-AC14.

---

### Phase 4 — .NET Solution Skeleton

- [x] [P4-T1] Create `TaskMaster.sln` at repo root via `dotnet new sln --name TaskMaster`. Verify: `Test-Path TaskMaster.sln` is true. Capture to `evidence/qa-gates/p4-t1-sln-created.<timestamp>.txt` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. PASS: solution file exists. AC: AC15 (precondition).
- [x] [P4-T2] Create placeholder library project at `src/TaskMaster.Domain/TaskMaster.Domain.csproj` via `dotnet new classlib --name TaskMaster.Domain --output src/TaskMaster.Domain --framework net8.0`. Verify: csproj exists. Capture to `evidence/qa-gates/p4-t2-classlib.<timestamp>.txt`. PASS: csproj exists. AC: AC15 (precondition).
- [x] [P4-T3] Create `tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj` via `dotnet new xunit --name TaskMaster.ArchitectureTests --output tests/TaskMaster.ArchitectureTests --framework net8.0`. Verify: csproj exists; package reference to `xunit` present. Capture to `evidence/qa-gates/p4-t3-archtests.<timestamp>.txt`. PASS: csproj exists. AC: AC21 (precondition).
- [x] [P4-T4] Add both projects to the solution: `dotnet sln TaskMaster.sln add src/TaskMaster.Domain/TaskMaster.Domain.csproj tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj`. Verify: `dotnet sln TaskMaster.sln list` shows both. Capture to `evidence/qa-gates/p4-t4-sln-list.<timestamp>.txt`. PASS: both projects listed. AC: AC15 (precondition).
- [x] [P4-T5] Add ProjectReference from `tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj` to `src/TaskMaster.Domain/TaskMaster.Domain.csproj` via `dotnet add tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj reference src/TaskMaster.Domain/TaskMaster.Domain.csproj`. Verify: the test csproj contains the ProjectReference element. Capture to `evidence/qa-gates/p4-t5-projref.<timestamp>.txt`. PASS: reference present. AC: AC21 (precondition).

---

### Phase 5 — Directory.Build.props, Directory.Packages.props, .editorconfig, dotnet-tools.json

- [x] [P5-T1] Create `Directory.Build.props` at repo root with `<Nullable>enable</Nullable>`, `<TreatWarningsAsErrors>true</TreatWarningsAsErrors>`, `<AnalysisLevel>latest-all</AnalysisLevel>`, `<AnalysisMode>All</AnalysisMode>`, and `<LangVersion>latest</LangVersion>` inside a single `<PropertyGroup>` applied to all projects. Verify: `Select-String -Path 'Directory.Build.props' -Pattern '<Nullable>enable</Nullable>','<TreatWarningsAsErrors>true</TreatWarningsAsErrors>','<AnalysisLevel>latest-all</AnalysisLevel>','<AnalysisMode>All</AnalysisMode>'` returns four matches. Capture to `evidence/qa-gates/p5-t1-grep.<timestamp>.txt`. PASS: four matches. AC: AC15.
- [x] [P5-T2] Create `Directory.Packages.props` at repo root with `<ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>` and `<ItemGroup>` containing `<PackageVersion Include="..." Version="..." />` lines that pin (at minimum) the six analyzer packages and the test SDK packages introduced in P6 and P7. Verify: `Select-String -Path 'Directory.Packages.props' -Pattern 'ManagePackageVersionsCentrally','Meziantou\.Analyzer','SonarAnalyzer\.CSharp','Roslynator\.Analyzers','AsyncFixer','SecurityCodeScan\.VS2019','Microsoft\.CodeAnalysis\.BannedApiAnalyzers'`. Capture to `evidence/qa-gates/p5-t2-grep.<timestamp>.txt`. PASS: seven matches. AC: AC16.
- [x] [P5-T3] Create or update `.editorconfig` at repo root to include rules for: file-scoped namespaces (`csharp_style_namespace_declarations = file_scoped:error`), using-directive ordering (`dotnet_sort_system_directives_first = true`, `dotnet_separate_import_directive_groups = false`), and naming conventions for interfaces (`I` prefix), private fields (`_camelCase`), constants, and async method suffix `Async`. Verify: `Select-String -Path '.editorconfig' -Pattern 'csharp_style_namespace_declarations = file_scoped','dotnet_sort_system_directives_first = true','dotnet_naming_rule\.','async'` returns at least four matches. Capture to `evidence/qa-gates/p5-t3-grep.<timestamp>.txt`. PASS: four+ matches. AC: AC19.
- [x] [P5-T4] Create `.config/dotnet-tools.json` registering CSharpier as a local tool via `dotnet new tool-manifest` followed by `dotnet tool install csharpier`. Verify: `.config/dotnet-tools.json` exists and contains `"csharpier"` key. Capture to `evidence/qa-gates/p5-t4-toolmanifest.<timestamp>.txt`. PASS: manifest present, csharpier registered. AC: AC19.
- [x] [P5-T5] Run `dotnet tool restore` and capture output to `evidence/qa-gates/p5-t5-tool-restore.<timestamp>.txt` with required schema. PASS: EXIT_CODE 0. AC: AC19.
- [x] [P5-T6] Run `dotnet csharpier check .` and capture to `evidence/qa-gates/p5-t6-csharpier-check.<timestamp>.txt` with required schema. PASS: EXIT_CODE 0 against the empty skeleton (or auto-format and re-run until clean). AC: AC19.

---

### Phase 6 — Analyzer Stack + BannedSymbols.txt

- [x] [P6-T1] Add the six analyzer `<PackageReference>` entries (with `PrivateAssets="all"` and `IncludeAssets="runtime; build; native; contentfiles; analyzers"`) to a single shared item group declared in `Directory.Build.props` so they apply to all projects; use the versions pinned in `Directory.Packages.props`. Verify: `Select-String -Path 'Directory.Build.props' -Pattern 'Meziantou\.Analyzer','SonarAnalyzer\.CSharp','Roslynator\.Analyzers','AsyncFixer','SecurityCodeScan\.VS2019','Microsoft\.CodeAnalysis\.BannedApiAnalyzers','PrivateAssets="all"'` returns seven matches (six packages + PrivateAssets present). Capture to `evidence/qa-gates/p6-t1-grep.<timestamp>.txt`. PASS: seven matches. AC: AC17.
- [x] [P6-T2] Create `BannedSymbols.txt` at repo root with exactly these lines (one symbol per line): `T:System.DateTime.Now`, `T:System.DateTime.UtcNow`, `P:System.Random.Shared`, `M:System.Threading.Thread.Sleep(System.Int32)`, `M:System.Threading.Tasks.Task.Delay(System.Int32)`. Verify: `Get-Content BannedSymbols.txt | Measure-Object -Line` returns 5 and `Select-String -Path 'BannedSymbols.txt' -Pattern 'System\.DateTime\.Now','System\.DateTime\.UtcNow','System\.Random\.Shared','System\.Threading\.Thread\.Sleep','System\.Threading\.Tasks\.Task\.Delay'` returns five matches. Capture to `evidence/qa-gates/p6-t2-grep.<timestamp>.txt`. PASS: five lines, five matches. AC: AC18.
- [x] [P6-T3] Wire `BannedSymbols.txt` into the build for all projects by adding `<AdditionalFiles Include="$(MSBuildThisFileDirectory)BannedSymbols.txt" />` inside a shared item group in `Directory.Build.props`. Verify: `Select-String -Path 'Directory.Build.props' -Pattern 'BannedSymbols\.txt'` returns >=1 match. Capture to `evidence/qa-gates/p6-t3-grep.<timestamp>.txt`. PASS: match present. AC: AC18.
- [x] [P6-T4] Run `dotnet build TaskMaster.sln --no-incremental` against the empty skeleton and capture to `evidence/qa-gates/p6-t4-dotnet-build.<timestamp>.txt`. PASS: EXIT_CODE 0, zero warnings (because `TreatWarningsAsErrors=true`). If non-zero, iterate on Phase 5/6 configuration until clean. AC: AC17, AC18, AC26 (precondition).

---

### Phase 7 — Test SDK References

- [x] [P7-T1] Add `<PackageVersion>` entries for `xunit`, `xunit.runner.visualstudio`, `Microsoft.NET.Test.Sdk`, `FluentAssertions`, `NSubstitute`, `Microsoft.Extensions.TimeProvider.Testing`, `WireMock.Net`, `Testcontainers`, `Microsoft.AspNetCore.Mvc.Testing`, and `coverlet.collector` to `Directory.Packages.props` (central pins). Verify: `Select-String -Path 'Directory.Packages.props' -Pattern 'xunit','FluentAssertions','NSubstitute','Microsoft\.Extensions\.TimeProvider\.Testing','WireMock\.Net','Testcontainers','Microsoft\.AspNetCore\.Mvc\.Testing','coverlet\.collector'` returns >=8 matches. Capture to `evidence/qa-gates/p7-t1-grep.<timestamp>.txt`. PASS: >=8 matches. AC: AC20, AC22, AC23.
- [x] [P7-T2] Add `<PackageReference>` entries to `tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj` for `xunit`, `xunit.runner.visualstudio`, `Microsoft.NET.Test.Sdk`, `FluentAssertions`, `NSubstitute`, `NetArchTest.Rules`, and `coverlet.collector` (versions resolved centrally). Verify: `Select-String -Path 'tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj' -Pattern 'xunit','FluentAssertions','NSubstitute','NetArchTest\.Rules','coverlet\.collector'` returns five matches. Capture to `evidence/qa-gates/p7-t2-grep.<timestamp>.txt`. PASS: five matches. AC: AC20, AC21.
- [x] [P7-T3] Add `<PackageReference Include="Microsoft.Extensions.TimeProvider.Testing" />` to `tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj` so the package is anchored in the solution (required by AC23 even though no domain test consumes it yet). Verify: grep for `Microsoft\.Extensions\.TimeProvider\.Testing` in the csproj returns >=1 match. Capture to `evidence/qa-gates/p7-t3-grep.<timestamp>.txt`. PASS: match present. AC: AC23.
- [x] [P7-T4] Add `<PackageReference>` entries for `WireMock.Net`, `Testcontainers`, and `Microsoft.AspNetCore.Mvc.Testing` to the architecture test csproj (anchored use; not exercised in tests yet). Verify: grep for the three patterns in the csproj returns three matches. Capture to `evidence/qa-gates/p7-t4-grep.<timestamp>.txt`. PASS: three matches. AC: AC22.
- [x] [P7-T5] Run `dotnet restore TaskMaster.sln` and capture to `evidence/qa-gates/p7-t5-restore.<timestamp>.txt`. PASS: EXIT_CODE 0. AC: AC20, AC22, AC23.
- [x] [P7-T6] Run `dotnet build TaskMaster.sln --no-incremental` and capture to `evidence/qa-gates/p7-t6-build.<timestamp>.txt`. PASS: EXIT_CODE 0 with zero warnings. If non-zero, iterate until clean. AC: AC17, AC20, AC22, AC23, AC26 (precondition).

---

### Phase 8 — Architecture Tests with NetArchTest.Rules

- [x] [P8-T1] Add `tests/TaskMaster.ArchitectureTests/NoComArchitectureTests.cs` containing one `[Fact]` per rule category, asserting NetArchTest rules against the loaded assemblies of `TaskMaster.Domain` and any other discovered TaskMaster.* assemblies in the solution. Categories: (a) no project depends on `Microsoft.Office.Interop.Outlook`; (b) no project depends on `System.Windows.Forms`, `System.Web`, or `Microsoft.VisualBasic`; (c) projects whose namespace starts with `TaskMaster.Domain` do not depend on namespaces matching `TaskMaster.Infrastructure*`. Each fact uses `Types.InAssembly(...)` or `Types.InAssemblies(...)` and asserts `result.IsSuccessful.Should().BeTrue(...)` (FluentAssertions). File must be <= 500 lines. Verify: `Select-String -Path 'tests/TaskMaster.ArchitectureTests/NoComArchitectureTests.cs' -Pattern '\[Fact\]','Microsoft\.Office\.Interop\.Outlook','System\.Windows\.Forms','System\.Web','Microsoft\.VisualBasic','TaskMaster\.Infrastructure','NetArchTest'` returns >=7 matches. Capture to `evidence/qa-gates/p8-t1-grep.<timestamp>.txt`. PASS: three `[Fact]` declarations and all rule terms present. AC: AC21.
- [x] [P8-T2] Run `dotnet test tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj --no-build:$false --logger "trx;LogFileName=arch.trx"` and capture to `evidence/qa-gates/p8-t2-archtests.<timestamp>.txt` with required schema. PASS: EXIT_CODE 0, three tests passed, zero failed. AC: AC21, AC26 (precondition).

---

### Phase 9 — NSwag Wiring to artifacts/openapi/current.json

- [x] [P9-T1] Create a minimal API project at `src/TaskMaster.Api/TaskMaster.Api.csproj` (`dotnet new webapi --name TaskMaster.Api --output src/TaskMaster.Api --framework net8.0 --use-minimal-apis`) and add it to the solution. Verify: csproj exists; `dotnet sln TaskMaster.sln list` shows it. Capture to `evidence/qa-gates/p9-t1-api-created.<timestamp>.txt`. PASS: project present in solution.
- [x] [P9-T2] Add `<PackageVersion Include="NSwag.MSBuild" Version="..." />` to `Directory.Packages.props` (or a `NSwag.AspNetCore` runtime ref as appropriate) and add `<PackageReference Include="NSwag.MSBuild" />` to `src/TaskMaster.Api/TaskMaster.Api.csproj`. Verify: grep for `NSwag\.MSBuild` in both files returns matches. Capture to `evidence/qa-gates/p9-t2-grep.<timestamp>.txt`. PASS: two matches. AC: AC24 (precondition).
- [x] [P9-T3] Add a `<Target Name="GenerateOpenApi" AfterTargets="Build">` block in `src/TaskMaster.Api/TaskMaster.Api.csproj` that invokes the NSwag MSBuild task to emit `$(MSBuildThisFileDirectory)../../artifacts/openapi/current.json`. Verify: `Select-String -Path 'src/TaskMaster.Api/TaskMaster.Api.csproj' -Pattern 'GenerateOpenApi','artifacts/openapi/current\.json','NSwag'` returns three matches. Capture to `evidence/qa-gates/p9-t3-grep.<timestamp>.txt`. PASS: three matches. AC: AC24.
- [x] [P9-T4] Run `dotnet build TaskMaster.sln` and verify the OpenAPI artifact is emitted. Verify: `Test-Path artifacts/openapi/current.json` is true and the file is non-empty JSON (`Get-Content artifacts/openapi/current.json -Raw | ConvertFrom-Json`). Capture to `evidence/qa-gates/p9-t4-openapi-emit.<timestamp>.txt`. PASS: file exists, parses as JSON. AC: AC24.

---

### Phase 10 — PR Pipeline Workflow Extension (Stages 1-5)

- [x] [P10-T1] Create `.github/actions/dotnet-format/action.yml` that runs `dotnet tool restore` and `dotnet csharpier check .` on `windows-latest`. Verify: file exists; `Select-String -Path '.github/actions/dotnet-format/action.yml' -Pattern 'csharpier check'`. Capture to `evidence/qa-gates/p10-t1-grep.<timestamp>.txt`. PASS: match present. AC: AC25.
- [x] [P10-T2] Create `.github/actions/dotnet-build/action.yml` that runs `dotnet restore TaskMaster.sln` then `dotnet build TaskMaster.sln --no-restore --warnaserror`. Verify: file exists; grep for `dotnet build` and `--warnaserror` returns matches. Capture to `evidence/qa-gates/p10-t2-grep.<timestamp>.txt`. PASS: matches present. AC: AC25.
- [x] [P10-T3] Create `.github/actions/dotnet-architecture/action.yml` that runs `dotnet test tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj --no-build`. Verify: grep for `TaskMaster.ArchitectureTests` returns match. Capture to `evidence/qa-gates/p10-t3-grep.<timestamp>.txt`. PASS: match present. AC: AC25.
- [x] [P10-T4] Create `.github/actions/dotnet-test/action.yml` that runs `dotnet test TaskMaster.sln --collect:"XPlat Code Coverage" --no-build --results-directory TestResults/`. Verify: grep for `XPlat Code Coverage` returns match. Capture to `evidence/qa-gates/p10-t4-grep.<timestamp>.txt`. PASS: match present. AC: AC25.
- [x] [P10-T5] Edit `.github/workflows/pr-pipeline.yml` to add five new jobs (in order, each depending on the previous): `stage-1-dotnet-format` (uses `./.github/actions/dotnet-format`), `stage-2-dotnet-build` (uses `./.github/actions/dotnet-build`), `stage-3-dotnet-typecheck` (no-op job that documents that nullable analysis runs inside `dotnet build`; depends on `stage-2-dotnet-build`), `stage-4-dotnet-architecture` (uses `./.github/actions/dotnet-architecture`), `stage-5-dotnet-test` (uses `./.github/actions/dotnet-test`). Each job runs on `windows-latest` and `needs:` chains in order. Verify: `Select-String -Path '.github/workflows/pr-pipeline.yml' -Pattern 'stage-1-dotnet-format','stage-2-dotnet-build','stage-3-dotnet-typecheck','stage-4-dotnet-architecture','stage-5-dotnet-test'` returns five matches. Capture to `evidence/qa-gates/p10-t5-grep.<timestamp>.txt`. PASS: five matches. AC: AC25.
- [x] [P10-T6] Validate workflow YAML syntax: run `pwsh -NoProfile -Command "Get-Content .github/workflows/pr-pipeline.yml | ConvertFrom-Yaml"` (using available YAML tooling) or `actionlint` if installed. Capture to `evidence/qa-gates/p10-t6-yaml-validate.<timestamp>.txt`. PASS: EXIT_CODE 0. AC: AC25.

---

### Phase 11 — quality-tiers.yml Registration

- [x] [P11-T1] Edit `quality-tiers.yml` to register the three new .NET projects: `TaskMaster.Domain` (path `src/TaskMaster.Domain`, language `csharp`, tier `t2`), `TaskMaster.Api` (path `src/TaskMaster.Api`, language `csharp`, tier `t3`), `TaskMaster.ArchitectureTests` (path `tests/TaskMaster.ArchitectureTests`, language `csharp`, tier `t4`). Include `rationale:` strings on each entry. Verify: `Select-String -Path 'quality-tiers.yml' -Pattern 'TaskMaster\.Domain','TaskMaster\.Api','TaskMaster\.ArchitectureTests','language: csharp'` returns >=4 matches. Capture to `evidence/qa-gates/p11-t1-grep.<timestamp>.txt`. PASS: matches present. AC: AC30.
- [x] [P11-T2] Run the tier-classification validator: `pwsh -NoProfile -File .github/scripts/validate-quality-tiers.ps1`. Capture to `evidence/qa-gates/p11-t2-tier-validate.<timestamp>.txt`. PASS: EXIT_CODE 0. AC: AC30.

---

### Phase 12 — Empty-Skeleton CI Green Run (Final QA Loop)

Full toolchain loop for .NET. Restart from P12-T1 if any step fails or autofixes files.

- [x] [P12-T1] Format: `dotnet tool restore` then `dotnet csharpier check .`. Capture to `evidence/qa-gates/p12-t1-format.<timestamp>.txt`. PASS: EXIT_CODE 0. AC: AC26.
- [x] [P12-T2] Lint (analyzers run inside build): `dotnet build TaskMaster.sln --no-incremental` with `TreatWarningsAsErrors=true` in effect. Capture to `evidence/qa-gates/p12-t2-build.<timestamp>.txt`. PASS: EXIT_CODE 0, zero warnings. AC: AC26.
- [x] [P12-T3] Type-check (nullable analysis verified by build): record explicitly that nullable enforcement was active during P12-T2 via grep of `evidence/qa-gates/p12-t2-build.<timestamp>.txt` for `Nullable` warnings (expected zero). Capture summary to `evidence/qa-gates/p12-t3-typecheck.<timestamp>.txt`. PASS: zero nullable warnings recorded. AC: AC26.
- [x] [P12-T4] Architecture tests: `dotnet test tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj --no-build`. Capture to `evidence/qa-gates/p12-t4-architecture.<timestamp>.txt`. PASS: EXIT_CODE 0, three tests passed. AC: AC21, AC26.
- [x] [P12-T5] Unit tests with coverage: `dotnet test TaskMaster.sln --no-build --collect:"XPlat Code Coverage" --results-directory TestResults/`. Capture to `evidence/qa-gates/p12-t5-test-coverage.<timestamp>.txt`. `Output Summary:` must record the coverage headline (line %, branch %) parsed from the cobertura.xml under `TestResults/*/coverage.cobertura.xml`. PASS: EXIT_CODE 0; coverage values recorded. (Note: with the empty skeleton, applicable lines/branches may be 0; the artifact still records the numeric headline values per coverage evidence contract.) AC: AC26.
- [x] [P12-T6] Phase 12 closure gate: write `evidence/qa-gates/phase12-restart-gate.<timestamp>.md` listing the EXIT_CODE of P12-T1 through P12-T5 in a single pass. PASS: all five EXIT_CODE values are 0 in the same pass. AC: AC26.

---

### Phase 13 — Representative Violation Demonstrations

Each demonstration introduces a single violation in a scratch file, runs the gate, captures the failing output as evidence, then reverts the change. Use the `[expect-fail]` tag on the gate run.

- [ ] [P13-T1] Banned-API demonstration (introduce). Create `src/TaskMaster.Domain/_violations/BannedApiProbe.cs` containing `public static class BannedApiProbe { public static System.DateTime Probe() => System.DateTime.UtcNow; }`. Verify: file exists. Capture to `evidence/qa-gates/p13-t1-introduce.<timestamp>.txt`. PASS: file written. AC: AC27 (precondition).
- [ ] [P13-T2] [expect-fail] Banned-API demonstration (run). Execute `dotnet build TaskMaster.sln --no-incremental` and capture to `evidence/regression-testing/p13-t2-banned-api-build.<timestamp>.txt` with required schema. PASS: EXIT_CODE != 0 AND `Output Summary:` references the `RS0030` (or equivalent BannedApiAnalyzers) diagnostic identifier and `DateTime.UtcNow`. AC: AC27.
- [ ] [P13-T3] Banned-API demonstration (revert). Delete `src/TaskMaster.Domain/_violations/BannedApiProbe.cs` and its empty parent folder. Verify: `Test-Path src/TaskMaster.Domain/_violations/BannedApiProbe.cs` is false. Run `dotnet build TaskMaster.sln` and capture to `evidence/qa-gates/p13-t3-revert-build.<timestamp>.txt`. PASS: file absent; EXIT_CODE 0. AC: AC27.
- [ ] [P13-T4] Architecture-rule demonstration (introduce). Add a class `src/TaskMaster.Domain/_violations/InfraLeak.cs` with `namespace TaskMaster.Domain; public class InfraLeak { public string Leak() => typeof(System.Windows.Forms.Form).FullName!; }` and add `<PackageReference Include="..." />` only if required; otherwise the namespace reference alone should be enough to trigger the rule. (If System.Windows.Forms is unavailable on the SDK, substitute by adding a temporary new project `src/TaskMaster.Infrastructure.Probe/` and a ProjectReference from `TaskMaster.Domain` to it, then run.) Verify: violating source present. Capture to `evidence/qa-gates/p13-t4-introduce.<timestamp>.txt`. PASS: file written. AC: AC28 (precondition).
- [ ] [P13-T5] [expect-fail] Architecture-rule demonstration (run). Execute `dotnet test tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj` and capture to `evidence/regression-testing/p13-t5-arch-test.<timestamp>.txt` with required schema. PASS: EXIT_CODE != 0 AND `Output Summary:` references the failing fact name and either `System.Windows.Forms` or `TaskMaster.Infrastructure*` per the rule violated. AC: AC28.
- [ ] [P13-T6] Architecture-rule demonstration (revert). Remove `src/TaskMaster.Domain/_violations/InfraLeak.cs` and any temporary probe project + ProjectReference added in P13-T4. Verify file/project absence and `dotnet test tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj` exits 0. Capture to `evidence/qa-gates/p13-t6-revert.<timestamp>.txt`. PASS: revert verified; EXIT_CODE 0. AC: AC28.
- [ ] [P13-T7] Analyzer-rule demonstration (introduce). Add `src/TaskMaster.Domain/_violations/AnalyzerProbe.cs` containing an obvious analyzer violation that one of the six analyzers in the stack will flag at error severity (e.g., a Meziantou MA0006/MA0011-style violation, a Roslynator RCS-rule violation, or a SonarAnalyzer S-rule violation; choose one whose default severity under `AnalysisMode=All` plus `TreatWarningsAsErrors=true` will fail the build). Document the exact rule ID and rationale in `evidence/qa-gates/p13-t7-introduce.<timestamp>.txt`. Verify: file present and `Output Summary:` records the target diagnostic ID. PASS: file written, target ID documented. AC: AC29 (precondition).
- [ ] [P13-T8] [expect-fail] Analyzer-rule demonstration (run). Execute `dotnet build TaskMaster.sln --no-incremental` and capture to `evidence/regression-testing/p13-t8-analyzer-build.<timestamp>.txt`. PASS: EXIT_CODE != 0 AND `Output Summary:` references the diagnostic ID documented in P13-T7. AC: AC29.
- [ ] [P13-T9] Analyzer-rule demonstration (revert). Delete `src/TaskMaster.Domain/_violations/AnalyzerProbe.cs`. Verify absence; run `dotnet build TaskMaster.sln` and capture to `evidence/qa-gates/p13-t9-revert-build.<timestamp>.txt`. PASS: file absent; EXIT_CODE 0. AC: AC29.
- [ ] [P13-T10] Phase 13 closure gate: write `evidence/qa-gates/phase13-closure.<timestamp>.md` listing the six demonstration artifacts (3 expect-fail + 3 revert) and confirming the post-revert build is green. PASS: artifact references all six file paths and confirms green post-state. AC: AC27, AC28, AC29.

---

### Phase 14 — Acceptance-Criteria Check-Off

- [ ] [P14-T1] Re-run the consolidated forbidden-token grep used in Phase 0 (P0-T5) across all eight Phase 1-3 target files. Capture to `evidence/qa-gates/p14-t1-final-grep.<timestamp>.txt`. PASS: zero matches across all files. AC: AC1, AC8, AC10, AC14.
- [ ] [P14-T2] Re-run the full final QA loop: `dotnet tool restore`, `dotnet csharpier check .`, `dotnet build TaskMaster.sln --no-incremental`, `dotnet test tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj --no-build`, `dotnet test TaskMaster.sln --no-build --collect:"XPlat Code Coverage" --results-directory TestResults/`. Capture each command to a separate file under `evidence/qa-gates/p14-t2-*.<timestamp>.txt` with required schema. The coverage artifact `Output Summary:` must record numeric line and branch coverage values (post-change). PASS: every command EXIT_CODE 0 in a single pass; coverage headline recorded. AC: AC26.
- [ ] [P14-T3] Author `docs/features/active/2026-05-10-establish-dotnet-foundation-7/p14-acceptance-criteria-checkoff.md` per `.claude/skills/acceptance-criteria-tracking/SKILL.md`. The file MUST contain a table with one row per AC#1-AC#30, each row listing: the AC text (paraphrased), the producing task ID(s) (P#-T#), the evidence artifact path(s), and a status of PASS or FAIL. Verify: `Select-String -Path 'docs/features/active/2026-05-10-establish-dotnet-foundation-7/p14-acceptance-criteria-checkoff.md' -Pattern '^\| AC([1-9]|[12][0-9]|30) '` returns 30 distinct AC rows. Capture verification output to `evidence/qa-gates/p14-t3-acceptance-rows.<timestamp>.txt`. PASS: 30 rows present; every row status is PASS. AC: AC1-AC30 (reconciliation).
- [ ] [P14-T4] Final closure: write `evidence/qa-gates/phase14-closure.<timestamp>.md` summarizing all phase closure artifacts (P0-T9, P1-T9, P2-T5, P3-T9, P6-T4, P7-T6, P8-T2, P9-T4, P10-T6, P11-T2, P12-T6, P13-T10) and confirming `p14-acceptance-criteria-checkoff.md` reports 30/30 PASS. PASS: artifact written; references all twelve closure files plus the AC check-off file. AC: AC1-AC30 (reconciliation).
