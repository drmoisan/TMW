# AC13 — ELIMINATED (not applicable to this repo)

Timestamp: 2026-05-19T22-40

AC13 was removed from spec.md (version 0.2) by owner decision. The criterion
asserted a uniform `.codex/`/`.agents/`/`.github/` bundle mirror and a
"python + pester mirror-contract test suite." Neither exists in this
repository; the criterion was carried over from a different project context
(the VS Code extension repo that enforces bundle-contract tests).

Verified repo reality:
- `.codex/` contains `agents/`, `hooks/`, `prompts/` only — no `skills/` or `rules/` tree.
- No python or pester mirror-contract test suite exists under `tests/`.
- The mirrors that do exist (`.agents/skills/orchestrate`, `.agents/skills/feature-review-workflow`, `.github/skills/feature-review-workflow`) were kept byte-identical; see evidence/qa-gates/p6-t5-mirror-parity.md.

Action: AC13 eliminated. AC14 and AC15 numbering retained to preserve
references in existing audit artifacts. The legitimate mirror-parity work
recorded under P6-T3..T6 stands on its own and is unaffected.

Verdict: N/A (criterion eliminated).
