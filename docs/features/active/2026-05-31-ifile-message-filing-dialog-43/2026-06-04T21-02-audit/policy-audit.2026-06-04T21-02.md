# Policy Compliance Audit: iFile Message-Filing Dialog (#43) — Cycle 2 Reaudit

**Audit Date:** 2026-06-04
**Code Under Test:** Cycle-2 working-tree changes (uncommitted) on `feature/ifile-message-filing-dialog-43`, full branch diff vs `main` @ `0eb035f`:
- TypeScript (new): `src/taskpane/ifile/naa-token-acquirer.ts`, `src/taskpane/ifile/api-base-url.ts`
- TypeScript (modified): `src/taskpane/ifile/ifile.ts`, `src/taskpane/ifile/inline-host.ts`, `.dependency-cruiser.cjs`, `webpack.config.js`
- C# (modified): `src/TaskMaster.Api/Program.cs`, `src/TaskMaster.Api/TaskMaster.Api.csproj`
- Manifests/config: `manifest.json`, `manifest.xml`, `package.json`, `package-lock.json`
- Tests (new): `naa-token-acquirer.test.ts`, `ifile.bootstrap.test.ts`, `ifile.host-shell.test.ts`, `api-base-url.test.ts`; (modified) `inline-host.test.ts`
- Docs: `runbooks/entra-app-sso-config.runbook.md` (new), on-device verification runbook (modified)

**Coverage Metrics by Language:**

| Language | Files Changed | Tests | Test Result | Baseline Coverage | Post-Change Coverage | New Code Coverage |
|----------|--------------|-------|-------------|-------------------|---------------------|-------------------|
| TypeScript | 6 prod + 5 test | 132 tests | ✅ 132 pass, 0 fail | 96.00% lines, 95.00% branches | 96.48% lines, 95.47% branches | naa-token-acquirer.ts 98.14%; api-base-url.ts 100% |
| C# | 2 (Program.cs DI wiring; csproj whitespace) | not re-run this cycle | ❌ repo-wide below threshold | 21.99% lines, 8.01% branches | 21.99% lines, 8.01% branches (current artifact, Jun 4 14:30) | Program.cs 87.00% lines |
| JSON | 2 (manifest.json, package.json) | N/A | ✅ validation EXIT 0 | N/A | N/A | N/A |

### Coverage Evidence Checklist

- TypeScript baseline coverage artifact: prior-cycle green baseline (95.99% lines on the iFile package at cycle-1 exit; repo-wide baseline 96% lines per cycle-1 `final-ts-test-coverage.md`)
- TypeScript post-change coverage artifact: `coverage/lcov.info` (freshly regenerated by `npm run test:coverage` at 21:05)
- PowerShell baseline coverage artifact: `N/A - out of scope` (no PowerShell files changed this cycle)
- PowerShell post-change coverage artifact: `N/A - out of scope` (no PowerShell files changed this cycle)
- C# coverage artifact: `artifacts/csharp/coverage.xml` (present and current — Jun 4 14:30:55, generated ~1 min after the Program.cs edit at 14:29:46; repo-wide below threshold)
- Per-language comparison summary: section 1.2.1 below

**Fail-closed rule applied:** the C# coverage artifact is current (generated 14:30:55, ~1 minute after the Program.cs edit at 14:29:46) but reports repo-wide coverage far below threshold (21.99% lines / 8.01% branches) for a language with changed files. The shortfall is driven by unchanged backend packages (Infrastructure 9.83%, Application 18.8%), not by the cycle-2 Program.cs change (whose `Program` main class is 87% line-covered). Under the coverage-verification procedure, repo-wide below 80% is a FAIL trigger, so the C# coverage verdict is FAIL and the overall verdict is PARTIALLY COMPLIANT, not PASS.

---

## Rejected Scope Narrowing

The cycle context states the C# backend was "verification-only; review them only for regression."
Two C# files are present in the working-tree diff (`Program.cs`, `TaskMaster.Api.csproj`). Per the
non-negotiable scope invariant, a language with changed files cannot be marked out-of-scope,
regression-only, or informational-only. C# is therefore given an explicit coverage verdict (FAIL,
section 1.2.1). No other scope narrowing was attempted.

---

## Executive Summary

Cycle 2 replaces the legacy `Office.auth.getAccessToken` SSO path with nested app authentication
(NAA) via `@azure/msal-browser`, behind the existing host-neutral `TokenAcquirer` seam; adds a
runtime `isSetSupported("NestedAppAuth","1.1")` guard with a deterministic unsupported-environment
fallback; declares `webApplicationInfo`/`<WebApplicationInfo>` and the Dev Tunnel domains in both
manifests; splits the generic bootstrap error into stage-specific configuration/sign-in/connection
messages; and adds the HI-3 Entra SSO config runbook. All toolchain checks were re-run (not trusted
from the executor report).

**Policy documents evaluated:**
- ✅ `general-code-change.md`
- ✅ `general-unit-test.md` (incl. coverage-exclusion policy)
- ✅ `architecture-boundaries.md`, `typescript.md`, `typescript-suppressions.md`, `quality-tiers.md`, `tonality.md`

**Toolchain (re-run):** format:check ✅, lint ✅, typecheck ✅, depcruise ✅ (0 errors, 6 pre-existing orphan warnings), test:coverage ✅ (132/132), manifest validate ✅ + validate:xml ✅ (both EXIT 0).

**Key finding:** one FAIL — repo-wide C# coverage is below threshold (21.99% lines / 8.01% branches)
for a language with changed files (POL-1), driven by unchanged backend packages rather than the
cycle-2 change. All TypeScript policy checks pass.

**Temporary artifacts cleanup:**
- ✅ No temporary/throwaway scripts were created during this review.

---

## 1. General Unit Test Policy Compliance

### 1.1 Core Principles

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Independence** | ✅ PASS | Tests share no mutable state; `afterEach` restores mocks and unstubs globals; the Office global is deleted after default-boundary tests. |
| **Isolation** | ✅ PASS | Each test targets one behavior (silent success, popup fallback, unsupported rejection, non-interaction re-throw, default support check/predicate/constructor, stage-specific messages, URL-guard routing, dialog post-to-parent). |
| **Fast Execution** | ✅ PASS | Full TS suite 132 tests in ~8.7s; iFile cycle-2 files run in single-digit to low-tens of ms each. |
| **Determinism** | ✅ PASS | No real timers, no `Date.now`/`Math.random`/`setTimeout`; `fetch` and `Office` stubbed; fake MSAL instance injected. |
| **Readability & Maintainability** | ✅ PASS | Descriptive `it` names, explicit AAA comment sections. |

### 1.2 Coverage and Scenarios

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Baseline Coverage Documented** | ✅ PASS | Prior-cycle green baseline; cycle-2 repo-wide post-change measured fresh at 96.48% lines / 95.47% branches via `npm run test:coverage`. |
| **No Coverage Regression** | ✅ PASS (TS) | New seams (`bootstrap`, `runBootstrap`, NAA adapter) are in coverage, not excluded; `ifile.ts` 86.95% lines / 93.75% branches. |
| **New Code Coverage** | ✅ PASS (TS) | `naa-token-acquirer.ts` 98.14% lines / 100% branches; `api-base-url.ts` 100% lines / 90.9% branches. |
| **Comprehensive Coverage** | ✅ PASS (TS) | NAA supported/unsupported/popup/re-throw branches, default boundaries, and all three error stages tested. |
| **Positive Flows** | ✅ PASS | Silent-token success, supported-acquirer load-and-render, dialog post-to-parent. |
| **Negative Flows** | ✅ PASS | NAA-unsupported rejection, non-interaction re-throw, URL-guard mobile-loopback rejection, token-failure sign-in message. |
| **Edge Cases** | ✅ PASS | Malformed URL fall-back parsing in the loopback guard; default-predicate vs injected-predicate paths. |
| **Error Handling** | ✅ PASS | Token-acquisition failure and one-time-load failure both render visible, stage-specific error rows. |
| **Concurrency** | N/A | No concurrency surface in the cycle-2 code. |
| **State Transitions** | N/A | No stateful machine introduced. |

### 1.2.1 Per-Language Coverage Comparison

- TypeScript: Baseline: 96.00% lines -> Post-change: 96.48% lines. Change: +0.48% lines. New/changed-code coverage: 98.14% (naa-token-acquirer.ts), 100% (api-base-url.ts). Disposition: PASS. Evidence: `coverage/lcov.info`, `coverage/coverage-summary.json` (regenerated 21:05).
- C#: Baseline: 21.99% lines -> Post-change: 21.99% lines. Change: +0.00% lines (no cycle-2 regression; Program.cs change is included in this current artifact). New/changed-code coverage: 87.00% lines (Program main class, per the current artifact). Disposition: FAIL. Evidence: `artifacts/csharp/coverage.xml` (current, Jun 4 14:30:55, generated after the Program.cs edit at 14:29:46). Repo-wide C# coverage is 21.99% lines / 8.01% branches, below the 85% / 75% thresholds. The shortfall is driven by unchanged backend packages (Infrastructure 9.83%, Application 18.8%), not by the cycle-2 change. C# has changed files this cycle, so an explicit verdict is mandatory and repo-wide below 80% is a FAIL trigger (POL-1). The changed Program.cs lines themselves do not regress coverage (87% covered). Required remediation: run the full backend test suite (including the integration/Graph adapter tests) so the repo-wide C# figure reflects the complete suite, or record the backend-coverage shortfall as a pre-existing tracked item separate from this feature.

### 1.3 Test Structure and Diagnostics

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Clear Failure Messages** | ✅ PASS | Assertions check exact message constants and call counts. |
| **Arrange-Act-Assert Pattern** | ✅ PASS | Explicit Arrange / Act / Assert comments in every cycle-2 test. |
| **Document Intent** | ✅ PASS | Self-documenting `it` names and file-header docstrings. |

### 1.4 External Dependencies and Environment

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Avoid External Dependencies** | ✅ PASS | No real network, no real MSAL, no DB; `fetch` stubbed, MSAL faked. |
| **Use Mocks/Stubs** | ✅ PASS | Injected `createInstance`/`isNaaSupported`/`isInteractionRequired`; stubbed `fetch`/`Office`. |
| **Environment Stability** | ✅ PASS | No temporary files created; globals restored in `afterEach`. |

### 1.5 Policy Audit Requirement

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Pre-submission Review** | ✅ PASS | This document is the cycle-2 policy review. |

---

## 2. General Code Change Policy Compliance

### 2.1 Before Making Changes

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Clarify the objective** | ✅ PASS | Remediation-inputs 2026-06-04T20-29 defines the NAA token-path objective (OD-8). |
| **Read existing change plans** | ✅ PASS | `remediation-plan.2026-06-04T20-29.md` (30 tasks, all checked). |
| **Document the plan** | ✅ PASS | Plan + research basis recorded in the feature folder. |

### 2.2 Design Principles

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Simplicity first** | ✅ PASS | NAA logic isolated in one adapter; bootstrap seam unchanged in shape. |
| **Reusability** | ✅ PASS | Reuses the existing `TokenAcquirer` seam and `IFileController`. |
| **Extensibility** | ✅ PASS | All host/MSAL boundaries injected with defaults; future fallback flow has a named seam. |
| **Separation of concerns** | ✅ PASS | Pure URL guard (`api-base-url.ts`), host-bound auth (`naa-token-acquirer.ts`), Office-free `bootstrap`. |

### 2.3 Module & File Structure

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Cohesive modules** | ✅ PASS | One responsibility per module. |
| **Under 500 lines** | ✅ PASS | naa-token-acquirer.ts 154, ifile.ts 186, inline-host.ts 106, api-base-url.ts 61; largest test 234. |
| **Public vs internal** | ✅ PASS | MSAL narrowed to local `NestablePublicClient`/`TokenResult`; `Configuration` type-only. |
| **No circular dependencies** | ✅ PASS | depcruise `no-circular` reports 0 errors; `mountInline` takes the message by parameter to avoid `inline-host`→`ifile` cycle. |

### 2.4 Naming, Docs, and Comments

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Descriptive names** | ✅ PASS | camelCase locals, PascalCase types, kebab-case filenames. |
| **Docs/docstrings** | ✅ PASS | Module and function docstrings explain the seam and the NAA flow. |
| **Comment why, not what** | ✅ PASS | Comments explain the unsupported-branch rationale and the Office-free invariant. |

### 2.5 After Making Changes - Toolchain Execution

| Requirement | Status | Evidence |
|------------|--------|----------|
| **1. Formatting** | ✅ PASS | `npm run format:check` — "All matched files use Prettier code style". |
| **2. Linting** | ✅ PASS | `npm run lint` — clean exit. |
| **3. Type checking** | ✅ PASS | `npm run typecheck` (`tsc --noEmit`) — clean exit. |
| **4. Testing** | ✅ PASS | `npm run test:coverage` — 27 files / 132 tests pass. |
| **Architecture** | ✅ PASS | `npx depcruise src --config .dependency-cruiser.cjs` — 0 errors, 6 pre-existing orphan warnings. |
| **Full toolchain loop** | ✅ PASS | All TS stages green in a single pass. |
| **Explicit reporting** | ✅ PASS | Commands and results documented here. |

Note: `format:check` globs only `src/**/*.ts`; lint and tsc cover `tests/**` and pass. Pre-existing script-scope limitation (POL-3), not a cycle-2 regression.

### 2.6 Summarize and Document

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Summarize changes** | ✅ PASS | Remediation-inputs section 4 + plan. |
| **Design choices explained** | ✅ PASS | NAA dependency rationale evidence file documents OD-8 entailment. |
| **Update supporting documents** | ✅ PASS | HI-3 runbook added; on-device runbook updated; README updated. |
| **Provide next steps** | ✅ PASS | HI-1/HI-2/HI-3 human steps declared; remediation-inputs 21-02 lists POL-1 follow-up. |

---

## 3. Language-Specific Code Change Policy Compliance

### Section 3 (TypeScript) Code Change Policy Compliance

#### 3.1 Tooling & Baseline

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Formatting (Prettier)** | ✅ PASS | `npm run format:check` clean (src scope). |
| **Linting (ESLint)** | ✅ PASS | `npm run lint` clean. |
| **Type checking (TSC)** | ✅ PASS | `tsc --noEmit` clean. |
| **Testing (Vitest)** | ✅ PASS | 132/132 pass. |

#### 3.2 TypeScript Design & Typing

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Strong typing** | ✅ PASS | Exported APIs typed; MSAL narrowed to local interfaces; `Configuration` type-only. |
| **No suppressions** | ✅ PASS | No `eslint-disable`/`@ts-ignore`/`@ts-expect-error`/`@ts-nocheck` in any cycle-2 file (grep). |
| **ES modules** | ✅ PASS | ES module syntax throughout. |
| **Architecture boundaries** | ✅ PASS | `@azure/msal-browser` imported only by `naa-token-acquirer.ts` (grep over `src/`); depcruise rule `ifile-pure-modules-no-host-deps` extended to forbid MSAL in pure modules; 0 errors. |

#### 3.3 TypeScript Error Handling

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Specific errors / fail fast** | ✅ PASS | URL guard and NAA-unsupported branch throw/reject specific errors; non-interaction errors re-thrown. |
| **No silent failures** | ✅ PASS | Each catch renders a visible stage-specific error row; `runBootstrap` also logs as last resort. |

### New Runtime Dependency Policy — PASS

One new runtime dependency: `@azure/msal-browser ^5.11.0` (resolved 5.11.0), under `dependencies`. It is the official Microsoft NAA library implementing `createNestablePublicClientApplication`, entailed by OD-8 and the only Outlook-iOS-supported token path per the research. Rationale documented at `evidence/other/naa-dependency-rationale.md`. The `package.json` devDependency diff is an alphabetical reordering, not new packages.

### C# Code Change (regression review) — correctness PASS; coverage FAIL

`Program.cs` chains `.AddMicrosoftIdentityWebApi(...).EnableTokenAcquisitionToCallDownstreamApi().AddMicrosoftGraph().AddInMemoryTokenCaches()` (correct OBO enablement; no secret embedded). `csproj` change is whitespace only. Coverage verdict is FAIL (POL-1, section 1.2.1) due to repo-wide C# coverage below threshold; the changed Program.cs lines are 87% covered.

### Secrets — PASS

No client secret or token value committed. Manifests and the NAA adapter carry only the non-secret client ID, App ID URI, tenant ID, and Dev Tunnel hostnames. Backend OBO is wired from `GetSection("AzureAd")`; the csproj `UserSecretsId` GUID is the correct out-of-source secret mechanism. Grep for secret-like tokens across `src/TaskMaster.Api` found only the non-secret `UserSecretsId` and a user-secrets setup comment.

### modified-workflow-needs-green-run — NOT TRIGGERED

No `.github/workflows/**` file changed this cycle (`git diff --name-only`).

### Evidence Location Compliance — PASS

`git diff` (committed and working tree) shows no files under `artifacts/baselines/`, `artifacts/qa/`, `artifacts/evidence/`, or `artifacts/coverage/`. Cycle-2 evidence is under the canonical `<feature>/evidence/<kind>/` tree. No `EVIDENCE_LOCATION_OVERRIDE_REJECTED` condition arose.

---

## 4. Language-Specific Unit Test Policy Compliance

### Section 4 (TypeScript / Vitest)

#### 4.1 Framework and Scope

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Use Vitest** | ✅ PASS | All cycle-2 tests are Vitest `*.test.ts`. |
| **Coverage expectation** | ✅ PASS | Repo-wide 96.48% lines / 95.47% branches; new code >= 98%. |

#### 4.2 Test Style and Structure

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Focused unit tests** | ✅ PASS | One behavior per test. |
| **Mocking sparingly** | ✅ PASS | Inject fakes only at host/MSAL/network boundaries. |
| **Organization (tests/ mirrors src/)** | ✅ PASS | Tests live under `tests/taskpane/ifile/`, mirroring `src/taskpane/ifile/`. |

#### 4.3 Naming and Readability

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Naming conventions** | ✅ PASS | Descriptive `it` strings. |
| **Docstrings/comments** | ✅ PASS | File-header docstrings describe scope and seam. |

#### 4.4 Coverage-Exclusion Policy

| Requirement | Status | Evidence |
|------------|--------|----------|
| **No production file excluded** | ✅ PASS | `vitest.config.ts` `coverage.include = src/**/*.ts`; all `exclude` entries are permitted non-production paths (node_modules, dist, lib, lib-amd, `**/*.test.ts`, `src/test-support/**`, four config files). No `src/` production path excluded. |

---

## 5. Test Coverage Detail

### naa-token-acquirer.ts (8 tests)

| Test Name | Scenario Type | Status |
|-----------|--------------|--------|
| resolves silent token when NAA supported | Positive | ✅ |
| falls back to popup on interaction-required | Positive/Branch | ✅ |
| invokes fallback and rejects when NAA unsupported | Negative | ✅ |
| re-throws non-interaction silent error | Error Handling | ✅ |
| default Office support check reports unsupported | Negative (default boundary) | ✅ |
| default support check + default predicate (supported) | Positive (default boundary) | ✅ |
| builds MSAL instance via default constructor seam | Positive (default boundary) | ✅ |
| default predicate re-throws non-interaction error | Error Handling (default boundary) | ✅ |

**Coverage:** 98.14% lines / 100% branches (uncovered line 83 is the type-adapter arrow on the real MSAL constructor, unreachable without a real broker).

### ifile.ts bootstrap / runBootstrap (5 + 7 tests)

| Test Name | Scenario Type | Status |
|-----------|--------------|--------|
| sign-in error on token failure | Error Handling | ✅ |
| connection error on load failure | Error Handling | ✅ |
| distinct sign-in vs connection messages | Negative | ✅ |
| positive render on token+load success | Positive | ✅ |
| configuration message on URL-guard reject | Negative | ✅ |
| host shell loads + renders (desktop) | Positive | ✅ |
| dialog post-to-parent on row click | Positive | ✅ |
| sign-in message when NAA unsupported | Negative | ✅ |

**Coverage:** ifile.ts 86.95% lines / 93.75% branches (uncovered 174-182 is the host-bound `Office.onReady` registration, in the denominator, not excluded).

---

## 6. Test Execution Metrics

| Metric | Value | Status |
|--------|-------|--------|
| Total Tests | 132 | ✅ |
| Tests Passed | 132 (100%) | ✅ |
| Tests Failed | 0 | ✅ |
| Execution Time | ~8.7s total | ✅ Fast |
| Code Coverage (repo-wide, TS) | 96.48% lines, 95.47% branches | ✅ |

---

## 7. Code Quality Checks

**For TypeScript:**

| Check | Command | Result | Status |
|-------|---------|--------|--------|
| Prettier (src) | `npm run format:check` | clean | ✅ |
| ESLint | `npm run lint` | clean | ✅ |
| TSC | `npm run typecheck` | clean | ✅ |
| Vitest | `npm run test:coverage` | 132/132 | ✅ |
| dependency-cruiser | `npx depcruise src --config .dependency-cruiser.cjs` | 0 errors, 6 warn | ✅ |
| Manifest validate | `npm run validate` / `validate:xml` | EXIT 0 / EXIT 0 | ✅ |

**Notes:** The 6 depcruise warnings are pre-existing `no-orphans` (`severity: "warn"`, non-blocking). Repo-wide C# coverage below threshold is the only FAIL.

---

## 8. Gaps and Exceptions

### Identified Gaps
- **POL-1 (FAIL):** Repo-wide C# coverage is 21.99% lines / 8.01% branches (current artifact `artifacts/csharp/coverage.xml`, Jun 4 14:30:55), below 85% / 75%, for a language (C#) with changed files this cycle. The shortfall is in unchanged backend packages (Infrastructure 9.83%, Application 18.8%); the changed Program.cs DI wiring is 87% line-covered (no regression). Plan: run the full backend suite (including integration/Graph adapter tests) so the repo-wide figure reflects the complete suite, or record the backend-coverage shortfall as a pre-existing tracked item separate from this feature.
- **POL-2 (Observation):** `ifile-pure-modules-no-host-deps` `from` set does not include `ifile.ts`; a future direct MSAL import into the bootstrap module would not be caught. `ifile.ts` is currently MSAL-free.
- **POL-3 (Observation):** `format:check` globs only `src/**/*.ts`, not `tests/**`; lint and tsc cover the test tree and pass.

### Approved Exceptions
- HI-1 (admin consent), HI-2 (mobile build + on-device verification), HI-3 (Entra app NAA + OBO config) are declared human-execution exceptions with runbooks. Justification: tenant-admin/Global-Admin and physical-device actions are not executable from CI.

### Removed/Skipped Tests
- **None.** No tests removed or skipped.

---

## 9. Summary of Changes

### Commits / Working Tree
Cycle-2 changes are in the working tree (uncommitted) atop the 5-commit branch ahead of `main` @ `0eb035f`.

### Files Modified (cycle 2)
1. `src/taskpane/ifile/naa-token-acquirer.ts` (NEW) — NAA adapter, only MSAL importer.
2. `src/taskpane/ifile/api-base-url.ts` (NEW) — pure backend-URL reachability guard.
3. `src/taskpane/ifile/ifile.ts` (MODIFIED) — stage-specific messages; NAA acquirer wiring; Office-free bootstrap seam.
4. `src/taskpane/ifile/inline-host.ts` (MODIFIED) — injectable load-failure message.
5. `.dependency-cruiser.cjs` (MODIFIED) — MSAL added to forbidden set for pure modules.
6. `manifest.json` / `manifest.xml` (MODIFIED) — WebApplicationInfo + Dev Tunnel domains.
7. `package.json` / `package-lock.json` (MODIFIED) — `@azure/msal-browser ^5.11.0`.
8. `src/TaskMaster.Api/Program.cs` (MODIFIED) — OBO/Graph DI wiring.
9. `runbooks/entra-app-sso-config.runbook.md` (NEW) — HI-3 exception runbook.

---

## 10. Compliance Verdict

### Overall Status: ⚠️ PARTIALLY COMPLIANT

All TypeScript code-change, unit-test, architecture, suppression, dependency, secrets, and
evidence-location policies pass. One FAIL: repo-wide C# coverage is below threshold for a language
with changed files (POL-1), driven by unchanged backend packages rather than the cycle-2 change. The
targeted NAA/manifest/error-message remediation is complete and verified.

### Policy-by-Policy Summary

#### General Code Change Policy (Section 2)
- ✅ All subsections pass.

#### Language-Specific Code Change Policy (Section 3)
- ✅ TypeScript: tooling, design/typing, error handling pass.
- ⚠️ C#: wiring correct; repo-wide coverage below threshold from unchanged packages (POL-1).

#### General Unit Test Policy (Section 1)
- ✅ Core principles, scenarios, structure, dependencies, audit pass.
- ⚠️ Coverage: TS PASS; C# FAIL (POL-1).

#### Language-Specific Unit Test Policy (Section 4)
- ✅ TypeScript/Vitest: framework, style, naming, coverage-exclusion pass.

### Metrics Summary
- ✅ 132/132 tests passing (100%)
- ✅ 96.48% line / 95.47% branch coverage (TS, repo-wide)
- ❌ Repo-wide C# coverage below threshold from unchanged backend packages (POL-1)
- ✅ All TS code-quality checks passing

### Recommendation

**Needs revision** — run the full backend test suite so the repo-wide C# coverage figure reflects
the complete suite, then re-evaluate the C# verdict; or record the backend-coverage shortfall as a
pre-existing tracked item. No code defect was found in the cycle-2 NAA/manifest/error-message work,
and the changed Program.cs lines are 87% covered.

### Verdict Counts
- FAIL findings: **1** (POL-1)
- Blocking-PARTIAL findings: **0**
- policy-audit blocking_count = **1**

---

## Appendix A: Test Inventory

Cycle-2 TypeScript tests (Vitest), `tests/taskpane/ifile/`:

1. naa-token-acquirer.test.ts › resolves the silent access token when NAA is supported
2. naa-token-acquirer.test.ts › falls back to acquireTokenPopup on an interaction-required error
3. naa-token-acquirer.test.ts › invokes the documented fallback and rejects when NAA is unsupported
4. naa-token-acquirer.test.ts › re-throws a non-interaction silent error without calling popup
5. naa-token-acquirer.test.ts › default Office support check reports unsupported when NAA is absent
6. naa-token-acquirer.test.ts › default support check when supported + default interaction predicate
7. naa-token-acquirer.test.ts › builds the MSAL instance via the default constructor seam
8. naa-token-acquirer.test.ts › default interaction predicate re-throws a non-interaction error
9. ifile.bootstrap.test.ts › binds the handler and surfaces a visible error when token acquisition fails
10. ifile.bootstrap.test.ts › binds the handler and surfaces a visible error when the one-time load fails
11. ifile.bootstrap.test.ts › renders the distinct sign-in message when token acquisition fails
12. ifile.bootstrap.test.ts › renders the distinct connection message when the one-time load fails
13. ifile.bootstrap.test.ts › renders results on input when token and load both succeed
14. ifile.host-shell.test.ts › logs and returns without throwing when the host DOM is missing
15. ifile.host-shell.test.ts › renders a visible error and keeps the box responsive when the URL guard rejects
16. ifile.host-shell.test.ts › renders the distinct configuration message when the build URL guard rejects
17. ifile.host-shell.test.ts › loads folders and renders results on input for a reachable desktop build
18. ifile.host-shell.test.ts › posts the selection to the parent in the dialog presentation on row click
19. ifile.host-shell.test.ts › loads results when the NAA-supported acquirer resolves a token
20. ifile.host-shell.test.ts › renders the sign-in message and stays responsive when NAA is unsupported
21. api-base-url.test.ts › (6 cases: desktop passthrough, mobile loopback rejection, malformed-URL fall-back, IPv6 loopback, etc.)
22. inline-host.test.ts › (9 cases: render results, render error, mount binds handler before load, injected load-failure message, etc.)

Full repository suite: 27 files / 132 tests, all passing.

---

## Appendix B: Toolchain Commands Reference

```bash
# Formatting (src scope)
npm run format:check

# Linting
npm run lint

# Type checking
npm run typecheck

# Architecture boundaries
npx depcruise src --config .dependency-cruiser.cjs

# Tests + coverage
npm run test:coverage

# Manifest validation
npm run validate          # manifest.json
npm run validate:xml      # manifest.xml
```

C# (not re-run this cycle; repo-wide coverage below threshold — POL-1):

```bash
# Run the full backend suite (incl. integration/Graph adapter tests) to refresh repo-wide C# coverage
dotnet test /p:CollectCoverage=true /p:CoverletOutputFormat=cobertura
```

---

**Audit Completed By:** feature-review agent
**Audit Date:** 2026-06-04
**Policy Version:** Current (as of audit date)
