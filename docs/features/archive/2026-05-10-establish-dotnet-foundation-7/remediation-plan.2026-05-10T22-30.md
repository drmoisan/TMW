# Remediation Plan — Issue #7 (Prompt C1 — Establish .NET Foundation)

- Timestamp: 2026-05-10T22-30
- Feature folder: `docs/features/active/2026-05-10-establish-dotnet-foundation-7/`
- Canonical issue: #7
- Evidence root: `docs/features/active/2026-05-10-establish-dotnet-foundation-7/evidence/`
- Source inputs:
  - `docs/features/active/2026-05-10-establish-dotnet-foundation-7/remediation-inputs.2026-05-10T22-30.md`
  - `docs/features/active/2026-05-10-establish-dotnet-foundation-7/feature-audit.2026-05-10T22-30.md`
  - `docs/features/active/2026-05-10-establish-dotnet-foundation-7/code-review.2026-05-10T22-30.md`
  - `docs/features/active/2026-05-10-establish-dotnet-foundation-7/policy-audit.2026-05-10T22-30.md`
  - `docs/features/active/2026-05-10-establish-dotnet-foundation-7/issue.md` (Acceptance Criteria AC1-AC30)
  - `docs/features/active/2026-05-10-establish-dotnet-foundation-7/plan.md` (all Phase 0-14 tasks `[x]`)
- Plan-path continuity: this file is updated in place across preflight revision loops.

## Findings Addressed

- F1/R1 — New C# production files have 0% effective coverage (Blocker). Addressed by Phase R1 (new xUnit test project, `Microsoft.AspNetCore.Mvc.Testing` against `/health`, direct unit tests for `HealthResponse` and `AssemblyMarker`, coverage verification per file).
- F2/R2 — Canonical C# coverage artifact `artifacts/csharp/coverage.xml` is absent (Blocker). Addressed by Phase R2 (copy-step in `.github/actions/dotnet-test/action.yml` and equivalent local QA-gate step).
- F3/R3 — NSwag emission silently suppressed (Major). Addressed by Phase R3 via option (a) gated property `EnableNSwagEmission` defaulting `false` until upstream net10 support lands, with `ContinueOnError`/`IgnoreExitCode` removed and a TODO comment recorded. The hand-authored `artifacts/openapi/current.json` remains the interim source of truth.
- F4/R4 — `DomainProjectDoesNotDependOnInfrastructure` not negative-tested (Major). Addressed by Phase R4 (deterministic expect-fail demonstration distinct from the Microsoft.VisualBasic fact already used in P13-T5).

Minor / Info findings R5, R6, R7, R8 are deferred per remediation-inputs (defer-acceptable); see Phase R6 deferral record.

## Conventions

- Evidence artifacts MUST be written under the canonical evidence root above per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Forbidden locations (`artifacts/baselines/`, `artifacts/baseline/`, `artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/evidence/`, `artifacts/coverage/`, `artifacts/post-change/`, `artifacts/regression-testing/`) MUST NOT be used. Note: `artifacts/openapi/current.json` and `artifacts/csharp/coverage.xml` are product/contract outputs, not evidence, and remain allowed at those paths.
- ISO-8601 timestamp format: `yyyy-MM-ddTHH-mm`. Substitute `<timestamp>` with the current value when executing.
- Every command-bearing task writes an evidence artifact containing `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- Coverage evidence on `Output Summary:` must record numeric line and branch percentages parsed from the cobertura XML (overall and, for R1, per-file values for `Program.cs`, `HealthResponse.cs`, `AssemblyMarker.cs`).
- File-size budget: no production/test/script file may exceed 500 lines.
- Toolchain order (per `.claude/rules/general-code-change.md`): format -> lint -> type-check -> architecture -> unit tests -> contract -> integration.
- Mirror discipline preserved; this remediation does not edit `.claude/rules/*.md` or `.github/instructions/*.instructions.md`, so no policy mirror obligations are triggered.

## Findings-to-Phase Map

- F1/R1 -> Phase R1
- F2/R2 -> Phase R2
- F3/R3 -> Phase R3
- F4/R4 -> Phase R4
- Final QA loop -> Phase R5
- Deferrals + acceptance reconciliation -> Phase R6

---

### Phase R0 — Remediation Baseline Capture

- [x] [PR0-T1] Read the policy stack in the exact order defined by `.claude/skills/policy-compliance-order/SKILL.md`: `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/architecture-boundaries.md`, `.claude/rules/csharp.md`, `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Also record an absence check for `CLAUDE.md` at repo root. Write `docs/features/active/2026-05-10-establish-dotnet-foundation-7/evidence/remediation-baseline/phase-r0-instructions-read.<timestamp>.md` with `Timestamp:`, `Policy Order:`, and explicit list of files read. PASS: artifact exists, lists the six policy files plus the CLAUDE.md absence record.
- [x] [PR0-T2] Re-read the four input artifacts (remediation-inputs, feature-audit, code-review, policy-audit) and the current `plan.md`. Write `evidence/remediation-baseline/phase-r0-inputs-read.<timestamp>.md` listing all five paths and confirming F1, F2, F3, F4 are in scope. PASS: artifact exists and references the four finding IDs.
- [x] [PR0-T3] Confirm presence of the three new production files under audit: `src/TaskMaster.Api/Program.cs`, `src/TaskMaster.Api/HealthResponse.cs`, `src/TaskMaster.Domain/AssemblyMarker.cs`. Write `evidence/remediation-baseline/phase-r0-production-files-present.<timestamp>.md` with `Path:` / `Exists:` per file. PASS: all three `Exists: true`.
- [x] [PR0-T4] Confirm absence of `tests/TaskMaster.Api.Tests/` and the canonical coverage artifact `artifacts/csharp/coverage.xml`. Command (PowerShell): `Test-Path tests/TaskMaster.Api.Tests; Test-Path artifacts/csharp/coverage.xml`. Write `evidence/remediation-baseline/phase-r0-target-absences.<timestamp>.md` with `SearchScope:`, `SearchPatterns: tests/TaskMaster.Api.Tests, artifacts/csharp/coverage.xml`, `SearchResult:` per item. PASS: both expected to be absent; artifact records actual state.
- [x] [PR0-T5] Capture baseline `dotnet test` with coverage to confirm pre-remediation per-file coverage state. Command: `dotnet test TaskMaster.sln --no-build --collect:"XPlat Code Coverage" --results-directory TestResults/`. After the run, parse the latest `TestResults/*/coverage.cobertura.xml` and record overall `line-rate`, `branch-rate`, `lines-valid`, `branches-valid`, plus per-file rates for `Program.cs`, `HealthResponse.cs`, `AssemblyMarker.cs`. Write `evidence/remediation-baseline/phase-r0-baseline-coverage.<timestamp>.txt` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording all numeric headlines. PASS: EXIT_CODE 0; `Output Summary:` includes baseline per-file values (expected: 0 covered lines on each of the three files, lines-valid=0 anomaly recorded).
- [x] [PR0-T6] Capture baseline content of `.github/actions/dotnet-test/action.yml` and `src/TaskMaster.Api/TaskMaster.Api.csproj` for diff reference. Write `evidence/remediation-baseline/phase-r0-files-baseline.<timestamp>.md` containing both file contents under labeled headers. PASS: artifact written.
- [x] [PR0-T7] Capture baseline content of `tests/TaskMaster.ArchitectureTests/NoComArchitectureTests.cs` (full file) for R4 diff reference. Write `evidence/remediation-baseline/phase-r0-archtests-baseline.<timestamp>.md`. PASS: artifact written.
- [x] [PR0-T8] Phase R0 closure: write `evidence/remediation-baseline/phase-r0-closure.<timestamp>.md` listing every Phase R0 artifact. PASS: artifact references PR0-T1 through PR0-T7.

---

### Phase R1 — Coverage for `Program.cs`, `HealthResponse.cs`, `AssemblyMarker.cs` (F1)

All R1 tasks resolve F1 / coverage gate on new production files. Test project tier classification: T4 (test code) for `TaskMaster.Api.Tests`; uniform tier coverage thresholds (line >= 85%, branch >= 75%) apply to the production files under test.

- [x] [PR1-T1] Create the test project `tests/TaskMaster.Api.Tests/TaskMaster.Api.Tests.csproj` via `dotnet new xunit --name TaskMaster.Api.Tests --output tests/TaskMaster.Api.Tests --framework net8.0`. Verify: `Test-Path tests/TaskMaster.Api.Tests/TaskMaster.Api.Tests.csproj` returns true. Capture to `evidence/qa-gates/pr1-t1-create-testproj.<timestamp>.txt` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. PASS: csproj exists; EXIT_CODE 0. Resolves: F1 (precondition).
- [x] [PR1-T2] Edit `tests/TaskMaster.Api.Tests/TaskMaster.Api.Tests.csproj` to add `<PackageReference>` entries for `Microsoft.AspNetCore.Mvc.Testing`, `Microsoft.NET.Test.Sdk`, `xunit`, `xunit.runner.visualstudio`, `FluentAssertions`, `NSubstitute`, and `coverlet.collector` (versions resolved centrally via `Directory.Packages.props`). Add `<ProjectReference Include="..\..\src\TaskMaster.Api\TaskMaster.Api.csproj" />` and `<ProjectReference Include="..\..\src\TaskMaster.Domain\TaskMaster.Domain.csproj" />`. Verify: `Select-String -Path 'tests/TaskMaster.Api.Tests/TaskMaster.Api.Tests.csproj' -Pattern 'Microsoft\.AspNetCore\.Mvc\.Testing','FluentAssertions','NSubstitute','coverlet\.collector','TaskMaster\.Api\.csproj','TaskMaster\.Domain\.csproj'` returns six matches. Capture to `evidence/qa-gates/pr1-t2-csproj-references.<timestamp>.txt`. PASS: six matches. Resolves: F1.
- [x] [PR1-T3] If `Microsoft.AspNetCore.Mvc.Testing` is not already pinned in `Directory.Packages.props` (it is per P7-T1) verify presence; otherwise add `<PackageVersion Include="Microsoft.AspNetCore.Mvc.Testing" Version="..." />`. Verify: `Select-String -Path 'Directory.Packages.props' -Pattern 'Microsoft\.AspNetCore\.Mvc\.Testing'` returns >= 1 match. Capture to `evidence/qa-gates/pr1-t3-packages-grep.<timestamp>.txt`. PASS: match present. Resolves: F1 (toolchain anchor).
- [x] [PR1-T4] Make `Program` visible to the test project by adding `<InternalsVisibleTo Include="TaskMaster.Api.Tests" />` inside an `<ItemGroup>` in `src/TaskMaster.Api/TaskMaster.Api.csproj`. Verify: `Select-String -Path 'src/TaskMaster.Api/TaskMaster.Api.csproj' -Pattern 'InternalsVisibleTo','TaskMaster\.Api\.Tests'` returns >= 2 matches. Capture to `evidence/qa-gates/pr1-t4-internalsvisibleto.<timestamp>.txt`. PASS: matches present. Resolves: F1.
- [x] [PR1-T5] Add the test project to the solution: `dotnet sln TaskMaster.sln add tests/TaskMaster.Api.Tests/TaskMaster.Api.Tests.csproj`. Verify: `dotnet sln TaskMaster.sln list` shows `TaskMaster.Api.Tests`. Capture to `evidence/qa-gates/pr1-t5-sln-list.<timestamp>.txt`. PASS: project listed; EXIT_CODE 0. Resolves: F1.
- [x] [PR1-T6] Create `tests/TaskMaster.Api.Tests/HealthEndpointTests.cs`. Content: an xUnit test class `HealthEndpointTests` using `IClassFixture<WebApplicationFactory<Program>>`. Add a `[Fact]` named `GetHealth_ReturnsOkAndStatusOk` that creates an HTTP client, GETs `/health`, asserts `HttpStatusCode.OK` via FluentAssertions, deserializes the response into `HealthResponse` (or equivalent shape), and asserts `Status == "ok"`. Add a second `[Fact]` named `GetHealth_ReturnsJsonContentType` that asserts the response `Content-Type` starts with `application/json`. File must be <= 500 lines. Verify: `Select-String -Path 'tests/TaskMaster.Api.Tests/HealthEndpointTests.cs' -Pattern '\[Fact\]','WebApplicationFactory<Program>','/health','HealthResponse','Status.*ok','application/json'` returns >= 6 matches. Capture to `evidence/qa-gates/pr1-t6-healthtests-grep.<timestamp>.txt`. PASS: matches present; file <= 500 lines. Resolves: F1 (Program.cs + HealthResponse.cs coverage).
- [x] [PR1-T7] Create `tests/TaskMaster.Api.Tests/HealthResponseTests.cs`. Content: an xUnit test class with at least two `[Fact]` tests directly constructing `HealthResponse` instances and asserting record-equality, property accessors (`Status`), and `ToString()` shape. The tests must cover positive instantiation and value-equality so branch coverage on the record's compiler-generated members is exercised. File must be <= 500 lines. Verify: `Select-String -Path 'tests/TaskMaster.Api.Tests/HealthResponseTests.cs' -Pattern '\[Fact\]','HealthResponse','Status'` returns >= 4 matches. Capture to `evidence/qa-gates/pr1-t7-healthresponse-grep.<timestamp>.txt`. PASS: matches present. Resolves: F1 (HealthResponse.cs direct unit coverage).
- [x] [PR1-T8] Create `tests/TaskMaster.Api.Tests/AssemblyMarkerTests.cs`. Content: an xUnit test class with at least two `[Fact]` tests: (a) `AssemblyName_EqualsDomainAssemblyName` asserts `TaskMaster.Domain.AssemblyMarker.AssemblyName == typeof(TaskMaster.Domain.AssemblyMarker).Assembly.GetName().Name`; (b) `AssemblyName_IsNonEmpty` asserts the constant is non-null and non-empty. File must be <= 500 lines. Verify: `Select-String -Path 'tests/TaskMaster.Api.Tests/AssemblyMarkerTests.cs' -Pattern '\[Fact\]','AssemblyMarker','AssemblyName'` returns >= 4 matches. Capture to `evidence/qa-gates/pr1-t8-assemblymarker-grep.<timestamp>.txt`. PASS: matches present. Resolves: F1 (AssemblyMarker.cs coverage).
- [x] [PR1-T9] Register the new test project in `quality-tiers.yml` as `TaskMaster.Api.Tests` (path `tests/TaskMaster.Api.Tests`, language `csharp`, tier `t4`) with a `rationale:` line. Verify: `Select-String -Path 'quality-tiers.yml' -Pattern 'TaskMaster\.Api\.Tests','tests/TaskMaster\.Api\.Tests','t4'` returns >= 3 matches. Capture to `evidence/qa-gates/pr1-t9-tier-registered.<timestamp>.txt`. PASS: matches present. Resolves: F1 (tier-classification gate per `.claude/rules/quality-tiers.md`).
- [x] [PR1-T10] Run the tier-classification validator: `pwsh -NoProfile -File .github/scripts/validate-quality-tiers.ps1`. Capture to `evidence/qa-gates/pr1-t10-tier-validate.<timestamp>.txt` with required schema. PASS: EXIT_CODE 0. Resolves: F1.
- [x] [PR1-T11] Run `dotnet restore TaskMaster.sln` and capture to `evidence/qa-gates/pr1-t11-restore.<timestamp>.txt` with required schema. PASS: EXIT_CODE 0. Resolves: F1 (build precondition).
- [x] [PR1-T12] Run `dotnet build TaskMaster.sln --no-incremental` and capture to `evidence/qa-gates/pr1-t12-build.<timestamp>.txt` with required schema. PASS: EXIT_CODE 0, zero warnings (TreatWarningsAsErrors active). If the build fails, iterate on PR1-T2/PR1-T4/PR1-T6-T8 until clean. Resolves: F1.
- [x] [PR1-T13] Run the new test project with coverage: `dotnet test tests/TaskMaster.Api.Tests/TaskMaster.Api.Tests.csproj --no-build --collect:"XPlat Code Coverage" --results-directory TestResults/`. Capture to `evidence/qa-gates/pr1-t13-test-coverage.<timestamp>.txt` with required schema. PASS: EXIT_CODE 0; tests passed count >= 6 (two health endpoint, two HealthResponse, two AssemblyMarker as authored). Resolves: F1.
- [x] [PR1-T14] Parse the latest `TestResults/*/coverage.cobertura.xml` and record per-file coverage for `Program.cs`, `HealthResponse.cs`, `AssemblyMarker.cs`. Command sketch (PowerShell): locate the newest cobertura file, load as XML, iterate `//class[@filename]` matching the three filenames, compute `line-rate` and `branch-rate` per file. Write `evidence/qa-gates/pr1-t14-per-file-coverage.<timestamp>.md` with one section per file containing `LineRate:` and `BranchRate:` as percentages, and an overall `PASS/FAIL` flag per file based on line >= 85% and branch >= 75%. PASS: all three files report line >= 85% and branch >= 75%. If any file fails, iterate on PR1-T6/T7/T8 to add scenarios. Resolves: F1 (final acceptance).

---

### Phase R2 — Canonical Coverage Artifact at `artifacts/csharp/coverage.xml` (F2)

- [x] [PR2-T1] Edit `.github/actions/dotnet-test/action.yml` to add a final composite step (after the `dotnet test` step) that copies the latest `TestResults/*/coverage.cobertura.xml` to `artifacts/csharp/coverage.xml`. Step body (PowerShell): `New-Item -ItemType Directory -Force -Path artifacts/csharp | Out-Null; Get-ChildItem TestResults -Recurse -Filter coverage.cobertura.xml | Sort-Object LastWriteTime -Descending | Select-Object -First 1 | Copy-Item -Destination artifacts/csharp/coverage.xml -Force`. The step must fail (non-zero exit) when no cobertura file is found. Verify: `Select-String -Path '.github/actions/dotnet-test/action.yml' -Pattern 'artifacts/csharp/coverage\.xml','coverage\.cobertura\.xml','Copy-Item'` returns >= 3 matches. Capture to `evidence/qa-gates/pr2-t1-action-edit-grep.<timestamp>.txt`. PASS: matches present. Resolves: F2.
- [x] [PR2-T2] Edit `.claude/skills/csharp-qa-gate/SKILL.md` to add a documented post-test step that performs the same copy locally (so local runs of the QA gate produce the canonical artifact). Verify: `Select-String -Path '.claude/skills/csharp-qa-gate/SKILL.md' -Pattern 'artifacts/csharp/coverage\.xml','coverage\.cobertura\.xml'` returns >= 2 matches. Capture to `evidence/qa-gates/pr2-t2-skill-grep.<timestamp>.txt`. PASS: matches present. (Mirror discipline: `.github/skills/csharp-qa-gate/` does not exist per plan P0-T4; no mirror edit required.) Resolves: F2.
- [x] [PR2-T3] Record the `.github/skills/csharp-qa-gate/` mirror absence: write `evidence/qa-gates/pr2-t3-mirror-absence.<timestamp>.md` with `SearchScope: .github/skills/csharp-qa-gate/`, `SearchPatterns: SKILL.md`, `SearchResult: none`. PASS: artifact written. Resolves: F2 (mirror discipline).
- [x] [PR2-T4] Update `.gitignore` if `artifacts/csharp/` is not yet ignored (review reported uncertain state). Command: `Select-String -Path '.gitignore' -Pattern 'artifacts/csharp'`; if no match, append a line `artifacts/csharp/`. Verify: `Select-String -Path '.gitignore' -Pattern 'artifacts/csharp'` returns >= 1 match. Capture to `evidence/qa-gates/pr2-t4-gitignore.<timestamp>.txt`. PASS: pattern present. Resolves: F2 (artifact hygiene).
- [x] [PR2-T5] Run the full local test + copy sequence end-to-end: `dotnet test TaskMaster.sln --no-build --collect:"XPlat Code Coverage" --results-directory TestResults/` followed by the copy step from PR2-T1 executed locally. Verify: `Test-Path artifacts/csharp/coverage.xml` returns true and the file parses as XML (`[xml](Get-Content artifacts/csharp/coverage.xml -Raw)`). Capture to `evidence/qa-gates/pr2-t5-canonical-coverage-emit.<timestamp>.txt` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording overall line-rate and branch-rate from the canonical file. PASS: file exists, parses, headline coverage values recorded. Resolves: F2.

---

### Phase R3 — NSwag Emission Loud-Fail Gating (F3)

Decision: option (a) from remediation-inputs — remove `ContinueOnError`/`IgnoreExitCode`, gate the target on an explicit opt-in property `EnableNSwagEmission` defaulting `false` until net10 launcher support lands. The hand-authored `artifacts/openapi/current.json` remains the interim source of truth, documented in the csproj TODO comment.

- [x] [PR3-T1] Edit `src/TaskMaster.Api/TaskMaster.Api.csproj`. In the `<PropertyGroup>`, add `<EnableNSwagEmission Condition="'$(EnableNSwagEmission)' == ''">false</EnableNSwagEmission>`. On the `<Target Name="GenerateOpenApi" AfterTargets="Build">` element: (a) add `Condition="'$(EnableNSwagEmission)' == 'true'"`, (b) remove the `ContinueOnError="true"` attribute, (c) remove the `IgnoreExitCode="true"` attribute from the `<Exec>` (or NSwag MSBuild task) child, (d) add a `<!-- TODO: Re-enable NSwag emission when net10 launcher support is available. Tracking: <NSwag GitHub issue URL>. Until then, artifacts/openapi/current.json is hand-authored. -->` comment immediately above the target. Verify: `Select-String -Path 'src/TaskMaster.Api/TaskMaster.Api.csproj' -Pattern 'EnableNSwagEmission','TODO: Re-enable NSwag'` returns >= 2 matches AND `Select-String -Pattern 'ContinueOnError="true"','IgnoreExitCode="true"'` returns zero matches. Capture to `evidence/qa-gates/pr3-t1-csproj-edit.<timestamp>.txt`. PASS: required patterns present, forbidden patterns absent. Resolves: F3.
- [x] [PR3-T2] Run `dotnet build TaskMaster.sln --no-incremental` (default state: `EnableNSwagEmission=false`) and verify the build succeeds and the NSwag target is skipped. Capture to `evidence/qa-gates/pr3-t2-build-default.<timestamp>.txt`. PASS: EXIT_CODE 0; `Output Summary:` records that GenerateOpenApi was skipped (target not executed). Resolves: F3.
- [x] [PR3-T3] [expect-fail] Run `dotnet build TaskMaster.sln -p:EnableNSwagEmission=true --no-incremental` to confirm the target now fails loudly when invoked under net10 (as expected per the documented incompatibility). Capture to `evidence/regression-testing/pr3-t3-nswag-loud-fail.<timestamp>.txt` with required schema. PASS (expect-fail): EXIT_CODE != 0 AND `Output Summary:` references the NSwag launcher failure (or, if upstream support has since landed and the build succeeds, record that fact and update PR3-T1 TODO comment in a follow-up). Resolves: F3 (loud-fail verification).
- [x] [PR3-T4] Document the interim authoritative source: append a short note to `docs/features/active/2026-05-10-establish-dotnet-foundation-7/evidence/other/pr3-t4-openapi-source-of-truth.<timestamp>.md` stating that `artifacts/openapi/current.json` is hand-authored, NSwag emission is gated off pending net10 launcher support, and listing the tracking reference written in the csproj TODO. PASS: artifact written and references the csproj TODO line. Resolves: F3.

---

### Phase R4 — Domain-vs-Infrastructure Architecture Fact Negative Test (F4)

The R4 negative test must exercise specifically the `DomainProjectDoesNotDependOnInfrastructure` fact (lines 69-87 of `NoComArchitectureTests.cs`) and must be distinct from the Microsoft.VisualBasic-style forbidden-namespaces fact used in P13-T5.

- [x] [PR4-T1] Create a temporary in-repo probe project `src/TaskMaster.Infrastructure.Probe/TaskMaster.Infrastructure.Probe.csproj` via `dotnet new classlib --name TaskMaster.Infrastructure.Probe --output src/TaskMaster.Infrastructure.Probe --framework net8.0`. Add a single public class `src/TaskMaster.Infrastructure.Probe/ProbeType.cs` with `namespace TaskMaster.Infrastructure.Probe; public class ProbeType { public static string Marker() => "probe"; }`. Add the project to the solution: `dotnet sln TaskMaster.sln add src/TaskMaster.Infrastructure.Probe/TaskMaster.Infrastructure.Probe.csproj`. Verify: csproj exists, source exists, solution lists the project. Capture to `evidence/qa-gates/pr4-t1-probe-introduce.<timestamp>.txt` with required schema. PASS: artifacts present. Resolves: F4 (precondition).
- [x] [PR4-T2] Introduce a deterministic typed reference from `TaskMaster.Domain` into `TaskMaster.Infrastructure.Probe`: add a temporary file `src/TaskMaster.Domain/_violations/InfraDependencyProbe.cs` with `namespace TaskMaster.Domain; internal static class InfraDependencyProbe { public static string Use() => TaskMaster.Infrastructure.Probe.ProbeType.Marker(); }`. Add a `<ProjectReference Include="..\TaskMaster.Infrastructure.Probe\TaskMaster.Infrastructure.Probe.csproj" />` to `src/TaskMaster.Domain/TaskMaster.Domain.csproj`. Verify: source file present and ProjectReference present. Capture to `evidence/qa-gates/pr4-t2-domain-leak-introduce.<timestamp>.txt`. PASS: file and reference present. Resolves: F4 (precondition).
- [x] [PR4-T3] If the existing assertion in `tests/TaskMaster.ArchitectureTests/NoComArchitectureTests.cs` lines 69-87 does not reliably detect this typed reference (per executor deviation #6), rewrite the `DomainProjectDoesNotDependOnInfrastructure` fact to use `Types.InAssembly(typeof(TaskMaster.Domain.AssemblyMarker).Assembly).That().ResideInNamespaceStartingWith("TaskMaster.Domain").Should().NotHaveDependencyOnAny(new[] { "TaskMaster.Infrastructure", "TaskMaster.Infrastructure.Probe" })` and assert `result.IsSuccessful.Should().BeFalse(...)` only in the expect-fail probe context — the production fact must still assert `BeTrue`. The rewrite preserves the original semantic (Domain must not depend on Infrastructure). File must remain <= 500 lines. Verify: `Select-String -Path 'tests/TaskMaster.ArchitectureTests/NoComArchitectureTests.cs' -Pattern 'ResideInNamespaceStartingWith.*TaskMaster\.Domain','NotHaveDependencyOnAny','TaskMaster\.Infrastructure'` returns >= 3 matches. Capture to `evidence/qa-gates/pr4-t3-arch-rewrite-grep.<timestamp>.txt`. PASS: matches present. Resolves: F4.
- [x] [PR4-T4] [expect-fail] Run `dotnet test tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj` with the probe and domain leak in place. Capture to `evidence/regression-testing/pr4-t4-domain-infra-expect-fail.<timestamp>.txt` with required schema. PASS (expect-fail): EXIT_CODE != 0 AND `Output Summary:` references `DomainProjectDoesNotDependOnInfrastructure` as the failing fact AND references `TaskMaster.Infrastructure` (or `TaskMaster.Infrastructure.Probe`) in the dependency list. This demonstration is deliberately distinct from the Microsoft.VisualBasic fact used in P13-T5. Resolves: F4.
- [x] [PR4-T5] Revert the probe: delete `src/TaskMaster.Domain/_violations/InfraDependencyProbe.cs`, remove the `<ProjectReference>` to `TaskMaster.Infrastructure.Probe` from `src/TaskMaster.Domain/TaskMaster.Domain.csproj`, remove the probe project from the solution (`dotnet sln TaskMaster.sln remove src/TaskMaster.Infrastructure.Probe/TaskMaster.Infrastructure.Probe.csproj`), and delete the `src/TaskMaster.Infrastructure.Probe/` directory. Verify all four absences: `Test-Path` on each path returns false. Capture to `evidence/qa-gates/pr4-t5-revert.<timestamp>.txt`. PASS: all paths absent. Resolves: F4 (cleanup).
- [x] [PR4-T6] Run `dotnet test tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj --no-build:$false` post-revert. Capture to `evidence/qa-gates/pr4-t6-post-revert-arch.<timestamp>.txt`. PASS: EXIT_CODE 0; all architecture facts pass (including the rewritten `DomainProjectDoesNotDependOnInfrastructure`). Resolves: F4.

---

### Phase R5 — Final Full QA Loop

Restart from PR5-T1 if any step fails or auto-fixes files.

- [x] [PR5-T1] Format: `dotnet tool restore` then `dotnet csharpier check .`. Capture to `evidence/qa-gates/pr5-t1-format.<timestamp>.txt` with required schema. PASS: EXIT_CODE 0. Resolves: AC26 (re-verification).
- [x] [PR5-T2] Lint (analyzers inside build): `dotnet build TaskMaster.sln --no-incremental` with `TreatWarningsAsErrors=true`. Capture to `evidence/qa-gates/pr5-t2-build.<timestamp>.txt`. PASS: EXIT_CODE 0, zero warnings. Resolves: AC26 (re-verification).
- [x] [PR5-T3] Type-check (nullable analysis verified inside build): grep `evidence/qa-gates/pr5-t2-build.<timestamp>.txt` for nullable warnings (expected zero). Write summary to `evidence/qa-gates/pr5-t3-typecheck.<timestamp>.txt`. PASS: zero nullable warnings recorded. Resolves: AC26 (re-verification).
- [x] [PR5-T4] Architecture tests: `dotnet test tests/TaskMaster.ArchitectureTests/TaskMaster.ArchitectureTests.csproj --no-build`. Capture to `evidence/qa-gates/pr5-t4-architecture.<timestamp>.txt`. PASS: EXIT_CODE 0; all architecture facts pass (count >= 3, including the rewritten R4 fact). Resolves: AC21, AC26 (re-verification), F4.
- [x] [PR5-T5] Unit tests with coverage: `dotnet test TaskMaster.sln --no-build --collect:"XPlat Code Coverage" --results-directory TestResults/`. Capture to `evidence/qa-gates/pr5-t5-test-coverage.<timestamp>.txt`. `Output Summary:` must record overall line% and branch% AND per-file values for `Program.cs`, `HealthResponse.cs`, `AssemblyMarker.cs` (each >= 85% line, >= 75% branch). PASS: EXIT_CODE 0; tests passed >= 6; per-file thresholds met. Resolves: F1, AC26 (re-verification).
- [x] [PR5-T6] Emit canonical coverage artifact: run the copy step from PR2-T1 (locally) and verify `Test-Path artifacts/csharp/coverage.xml` is true. Capture to `evidence/qa-gates/pr5-t6-canonical-coverage.<timestamp>.txt` with required schema; `Output Summary:` records the overall line% and branch% from the canonical file. PASS: file exists, parses, values recorded. Resolves: F2.
- [x] [PR5-T7] Phase R5 closure: write `evidence/qa-gates/phase-r5-restart-gate.<timestamp>.md` listing the EXIT_CODE of PR5-T1 through PR5-T6 in a single pass. PASS: all six EXIT_CODE values are 0 in the same pass. Resolves: AC26 (re-verification).

---

### Phase R6 — Reconciliation, Deferrals, and Acceptance Re-Check

- [x] [PR6-T1] Record minor/info deferrals explicitly. Write `evidence/other/pr6-t1-minor-deferrals.<timestamp>.md` with one section per item R5, R6, R7, R8 from remediation-inputs (redundant `<ImplicitUsings>`, empty `stage-3-dotnet-typecheck`, `--no-build` flag in `dotnet-test/action.yml`, plan/spec `T:` vs `P:` narrative mismatch for `Random.Shared`). Each section MUST state: (a) the finding ID and severity, (b) deferral rationale (cost vs blocker risk), (c) follow-up tracking note. PASS: artifact written; all four items documented. Resolves: R5, R6, R7, R8 (deferral records).
- [x] [PR6-T2] Re-validate AC reconciliation. Update `docs/features/active/2026-05-10-establish-dotnet-foundation-7/p14-acceptance-criteria-checkoff.md` to (a) add new rows or annotate existing rows for AC26 reflecting the post-remediation coverage state, (b) reference the new evidence artifacts produced in Phases R1-R5, (c) flip any previously-PARTIAL outcomes affected by F3/F4 to PASS with the new evidence paths. Verify: `Select-String -Path 'docs/features/active/2026-05-10-establish-dotnet-foundation-7/p14-acceptance-criteria-checkoff.md' -Pattern '^\| AC([1-9]|[12][0-9]|30) '` returns 30 distinct AC rows; all status cells are PASS. Capture verification output to `evidence/qa-gates/pr6-t2-acceptance-rows.<timestamp>.txt`. PASS: 30 rows present; every row status is PASS. Resolves: AC1-AC30 (reconciliation).
- [x] [PR6-T3] Final closure: write `evidence/qa-gates/phase-r6-closure.<timestamp>.md` summarizing remediation outcome with one section per finding (F1, F2, F3, F4) referencing the resolving task IDs and evidence paths, plus a deferrals summary referencing PR6-T1. PASS: artifact references all four finding sections and the deferrals record. Resolves: F1, F2, F3, F4.
